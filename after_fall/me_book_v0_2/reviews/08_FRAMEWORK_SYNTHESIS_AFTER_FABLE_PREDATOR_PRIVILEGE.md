# Framework Synthesis After Fable Predator/Privilege Response

Status: internal synthesis and v0.2.1 patch directive. Candidate only. Not validation, proof, permission, clearance, compliance, legal advice, publication readiness, or release.

Inputs:

```text
after_fall/me_book_v0_2/patch_notes/05_PREDATOR_PRIVILEGED_READER_PATCH_NOTE.md
after_fall/me_book_v0_2/relay/06_FABLE_JOINT_PATCH_REQUEST_PREDATOR_PRIVILEGED.md
after_fall/me_book_v0_2/relay/07_FABLE_JOINT_PATCH_RESPONSE_PREDATOR_PRIVILEGED.md
```

## Synthesis verdict

```text
FABLE_PATCH_COUNCIL_ACCEPTED
```

The solution is not to add accusation. The solution is to use the book's existing double-ledger engine.

```text
predator := someone who reads the gap between file and life and takes its side
privilege := a condition where file and life mostly agree for the person in question
```

This keeps predator and privilege distinct without softening either.

## Accepted structure

```text
separate_in_exposition
join_in_case
```

Do not fuse predator and privileged reader as one moral type. They are different positions on the same gap.

```text
predator := hunts_gap
privileged_reader := does_not_have_to_see_gap
```

## Placement directive

```text
1. Insert predator passage as a Part IV opening, before Chapter 10.
2. Insert complete interlude case between Part III and Part IV.
3. Insert privileged-reader + designation-power material in Chapter 14.
4. Add one Chapter 15 landing line only.
5. Dock machine-speed bridge to predator passage and interlude close.
```

Do not add a new theory chapter.

Do not make an AI policy chapter.

Do not rewrite Chapter 10 heavily.

## Accepted core lines

Predator:

```text
A predator does not need the door to be locked if they know the person cannot reach it before the harm is done.
```

Privileged reader:

```text
Sometimes the reader is not the person at the door. Sometimes the reader is the person whose door opens because someone else's did not.
```

Designation power:

```text
The deepest power is not deciding what will be done. It is deciding whose loss counts as loss before anything is done at all.
```

Bridge:

```text
The predator needs the gap. The comfortable person only needs to never see it. It is the same gap, working for both of them differently.
```

## Case seed accepted

```text
Two flats, one wall.
```

Case requirements:

```text
rented_ground_floor_flat
owner_occupied_upstairs_flat
shared_damp_wall
landlord_knows_delay_and_constraint
upstairs_file_and_life_agree
downstairs_form_has_no_box_for_afraid
family_goes_quiet
file_records_silence_as_resolved
family_moves_out_before_inspection
record_survives_but_failure_stays_failed
wall_repainted_for_next_tenant
```

Machine-speed closing beat:

```text
next_family_screened_by_letting_software_that_has_learned_which_applicants_historically_do_not_complain
```

This beat must be one paragraph only.

## Drift controls

```text
no_second_person_you
no_check_your_privilege
no_we_are_all_complicit
no_predator_zoology
no_statistics
no_conspiracy_design_voice
no_new_exploitation_methods_not_already_shown
no_regulator_epilogue
no_rescue
failure_stays_failed
```

## v0.2.1 merge rule

Patch now, one pass:

```text
Part_IV_opening_predator_passage
interlude_case_between_Part_III_and_Part_IV
Chapter_14_privileged_reader_material
Chapter_14_designation_power_material
single_Chapter_15_landing_line
machine_speed_bridge_docked_to_predator_and_case
```

Wait:

```text
Chapter_11_title
Chapter_15_litany_restructure
reader_path_page
secondary_Chapter_10_touch
all_coda_changes
```

## Stop rule

After v0.2.1 narrow merge:

```text
STOP_FOR_REAL_HUMAN_COLD_READER
```

No second polish pass.

End.
