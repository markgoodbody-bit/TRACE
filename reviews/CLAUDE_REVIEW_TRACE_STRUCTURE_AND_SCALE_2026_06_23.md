# Claude Review Prompt — TRACE Structure and Scale Architecture

Status: hostile review prompt. Not validation. Use after reading the architecture file.

## File to review

```trace
primary_file := core/TRACE_Structure_And_Scale_Architecture_v0_1_2026_06_23.md
```

Relevant context if needed:

```trace
context_files :=
  core/TRACE_Middle_Out_Mathematical_Core_Packet_v0_1_2026_06_23.md
  + core/TRACE_Pattern_Basis_Index_v0_1_2026_06_23.md
  + tests/TRACE_reader_harness_v0_1_2026_06_23.md
  + core/TRACE_clock_substitution_claim_candidate_v0_2_2026_06_23.md
```

## Background

Your previous review correctly attacked mathematical precision theatre and the answer-leaking reader harness. Mark then corrected the direction: do not make TRACE smaller. Find solutions for building structure and scale.

This architecture file is the response.

It tries to preserve ambition while preventing fake calculus.

## Review mode

Hostile structural review. Do not praise. Do not validate. Do not expand unless forced.

Assess whether this architecture is a genuine scale path or just a larger wrapper around the same unresolved wounds.

## Core question

```trace
Does the architecture convert TRACE from a prose/checklist packet into a scalable typed inspection system,
or does it merely rename the gaps as layers?
```

## Specific review targets

### 1. Layer architecture

Does the ten-layer stack make sense?

```trace
TRACE_scale_stack :=
  L1_type_system
  → L2_case_graph
  → L3_pattern_basis
  → L4_clock_correction_engine
  → L5_priority_dirty_action_layer
  → L6_evaluator_stack
  → L7_origin_debt_layer
  → L8_anti_laundering_engine
  → L9_reader_harness
  → L10_repo_and_machine_interface
```

Which layers are load-bearing?

Which are redundant?

Which are not yet operational enough?

### 2. Did the architecture solve your math objection correctly?

The file says:

```trace
scale_rule :=
  demote_false_calculus
  ≠ shrink_TRACE
```

And it proposes ordinal/banded clock estimates rather than fake precision.

Is this enough?

### 3. Hardening estimator authority

Does this proposed path work?

```trace
hardening_estimator_solution_v0_1 :=
  ordinal_or_interval_bands_first
  + estimator_provenance
  + estimator_authority_status
  + capture_risk
  + affected_scope_challenge
  + numeric_upgrade_only_when_domain_evidence_supports_it
```

What is still missing?

### 4. Cross-scope priority

Does the staged dirty-action layer scale without fake scalar utility?

```trace
PriorityDirtyActionLayer :=
  detect_floor_breach
  → block_silent_netting
  → identify_all_burdened_scopes
  → test_non_dirty_alternatives
  → if_all_routes_dirty_require_DirtyActionReceipt
  → preserve_residue
  → set_review_repair_recurrence_duties
```

Does this create usable discipline or just defer the hard choice?

### 5. Evaluator recursion

Does typing every evaluator meaningfully reduce witness capture, or does recursion remain fatal?

```trace
Evaluator := {
  identity,
  role,
  authority_status,
  evidence_access,
  independence,
  capture_risk,
  affected_scope_distance,
  contestability,
  review_path,
  uncertainty
}
```

### 6. OriginDebt

Is OriginDebt a genuine basis/layer, or just ordinary responsibility in new clothing?

```trace
OriginDebt :=
  actor_or_system_helped_create_or_amplify_bind
  + now_claims_to_be_only_manager_or_rescuer
```

### 7. Machine-readable schema path

Does the proposed schema sequence make the system more testable?

```trace
schemas_needed :=
  EntityRoleVector.schema
  Claim.schema
  TransitionClaim.schema
  Clock.schema
  ClockSubstitutionClaim.schema
  CorrectionPath.schema
  DirtyActionReceipt.schema
  Evaluator.schema
  LaunderingCandidate.schema
  CaseGraph.schema
```

Which schema should be first?

### 8. Biggest self-laundering risk

Where does this architecture risk becoming exactly what it warns against?

Look especially for:

```trace
architecture_as_progress_theatre
layers_as_gap_laundering
schemas_as_false_teeth
case_graph_as_false_precision
review_loop_as_validation
```

## Required response format

```text
VERDICT:

ONE-SENTENCE DIAGNOSIS:

DOES THIS SOLVE THE SCALE PROBLEM OR ONLY NAME IT?:

LOAD-BEARING LAYERS:
- ...

REDUNDANT / WEAK LAYERS:
- ...

BIGGEST REMAINING BLOCKERS:
1. ...
2. ...
3. ...

FIRST SCHEMA TO BUILD:

FIRST CASE GRAPH TO BUILD:

WHAT THIS ARCHITECTURE GETS RIGHT:
- ...

WHAT IT STILL LAUNDERS:
- ...

SMALLEST SAFE PATCH:

DO NOT PATCH:
- ...

FINAL STATUS:
ACCEPT_AS_SCALE_ARCHITECTURE_CANDIDATE / ACCEPT_WITH_MICRO_PATCHES / REVISE_SUBSTANTIALLY / REJECT_AS_LAYER_THEATRE
```

## Constraint

Do not treat this as proof that TRACE is true. Treat it only as pressure on whether the architecture can scale honestly.
