# TRACE Meta-Hypotheses — 2026-06-23

Status: continuity memory; not validation; not canon unless separately promoted.

Purpose: preserve the meta-level hypotheses produced during the Scale With Teeth / cold-transfer phase so future tabs do not lose the strategic frame, the danger controls, or the distinction between ambition and proof.

```trace
memory_type := meta_hypotheses
scope := TRACE/ME build strategy + epistemic control + resource-constrained development
validation := false
```

---

## H0. Resource context changes yield standard, not truth standard

```trace
resource_state :=
  one_person
  + no_institution
  + no_budget
  + no_lab
  + no_formal_review_channel
  + AI_tabs
  + GitHub
  + public_sources
  + evening_weekend_work
```

Hypothesis:

```trace
resource_context → yield_standard_adjustment
resource_context ↛ truth_standard_relaxation
```

Meaning: the amount of structured machinery generated with these resources can be judged unusually high; the claims themselves receive no epistemic discount.

Forbidden laundering:

```trace
one_person_no_budget ≠ validation
resource_constraint ≠ proof_substitute
```

---

## H1. The weird method may be rational under constraint

Hypothesis:

```trace
weird_method :=
  campfire_reasoning
  + glyph_compression
  + multi_AI_relay
  + hostile_review
  + repo_commits
  + schema_objects
  + case_graphs
  + cold_transfer_packets
  + falsifier_design
  + repeated_anti_overclaim_gates
```

Under ordinary conditions, this can look noisy or eccentric. Under Mark's constraints, it may be a rational way to generate structure quickly while preserving claim boundaries.

Falsifier:

```trace
if artifacts_accumulate_without_external_contact_or_failure_ledger:
  weird_method := self_sealing_build_loop
```

---

## H2. Internal yield is real but not external yield

Current strongest defensible phrase:

```trace
earned_word := unusually_disciplined
not := validated
not := proven
not := promising_without_teeth
```

Basis:

```trace
basis :=
  generates_self_falsifiers_faster_than_claims
  + absorbs_external_objections_into_next_artifact
  + records_forbidden_claims
  + creates_demotion_triggers
```

Caveat:

```trace
all_or_most_yield_so_far := internal
external_contact := limited
```

Falsifier:

```trace
if internal_controls_never_meet_external_disagreement:
  yield := closed_loop_discipline_only
```

---

## H3. Scale must continue, but every scale claim needs teeth

Hypothesis:

```trace
strategy :=
  build_large
  + label_evidence
  + attach_falsifiers
  + preserve_failure_ledger
  + avoid_validation_claim
```

Not:

```trace
shrink_to_harmless_tests_only
```

Rationale: the target problem is large — harm, authority, timing, correction, laundering, AI/institutional action. Tiny proof games alone can become honest but useless.

Guard:

```trace
ambition ≠ permission_to_overclaim
```

---

## H4. Cold format-transfer is not decision advantage

Current status:

```trace
cold_format_transfer_self_reported_once := true
self_scored_partial_pass := true
decision_advantage_over_competent_baseline := unproven
validation := false
```

This was registered as X057 in the claims-ledger delta.

Falsifier:

```trace
if blind_regrade_result == COMPRESSION_ONLY:
  demote_X057
```

Upgrade condition:

```trace
upgrade_requires :=
  blind_independent_regrade_shows_decision_delta
  + counterbalanced_order_run_replicates_delta
  + no_supplied_laundering_list
  + independent_grader
```

Forbidden claim:

```trace
20_24_self_score ≠ external_validation
```

---

## H5. Controls are the strongest portable contribution so far

Hypothesis:

```trace
strongest_transferable_IP := controls
```

Examples:

```trace
controls :=
  baseline_required
  + EvidenceStatus
  + DirectAffectedScopeWitness
  + DirtyDelayReceipt
  + claim_typing
  + clock_deadline_separation
  + laundering_scan
  + correction_path_realism
  + residue_ledger
  + blind_decision_delta_rubric
  + negative_controls
  + failure_ledger
```

Caveat:

```trace
controls_generated ≠ framework_valid
```

A disciplined falsification apparatus can still wrap a wrong idea.

---

## H6. DirtyDelayReceipt may be the first high-bite operator

Hypothesis:

```trace
DirtyDelayReceipt :=
  separates necessary delay
  from clean resolution
  while keeping delay burden on the ledger
```

Why it matters: it captures a real recurring problem where evidential/legal/safety delay may be legitimate but still harms affected people and must not be laundered as repair.

Falsifier:

```trace
if ordinary baseline consistently handles this distinction without TRACE:
  DirtyDelayReceipt := communication_label_only
```

---

## H7. EvidenceStatus and DirectAffectedScopeWitness constrain TRACE against its own harm

Hypothesis:

```trace
EvidenceStatus := anti_evidence_laundering
DirectAffectedScopeWitness := anti_speaking_over_harmed_people
```

These are valuable not because they are clever, but because they force the system to mark what it does not know and whose voice is absent.

Falsifier:

```trace
if outputs_include_fields_but_still_speak_over_people_or_upgrade_evidence_without_sources:
  fields := decorative
```

---

## H8. Negative controls are essential to prevent universal pattern capture

Hypothesis:

```trace
negative_controls_required := true
```

TRACE must show that it can idle when stakes are low, clocks are soft, authority asymmetry is minimal, and no protected-scope collapse is present.

Falsifier:

```trace
if TRACE_generates_large_deltas_on_mundane_low_stakes_cases:
  TRACE := pattern_overfit
```

---

## H9. The flat-tree / canon fork is a structural drift risk

Hypothesis:

```trace
repo_drift_risk :=
  numbered_canon_tree
  + newer_flat_tree
  + live_work_not_fully_wired_into_control_index
```

Danger: new artifacts accumulate outside claims/demotion discipline and become de facto canon.

Needed control:

```trace
control_index_patch_required := true
```

Falsifier:

```trace
if current_control_index_cannot_identify_authority_status_of_new_files:
  repo_state := fork_drift
```

---

## H10. Public surfaces must shed decorative friction

Hypothesis:

```trace
external_surface :=
  plain_roles
  + examples
  + test_protocols
  + evidence_status
  + failure_conditions
```

Demote externally:

```trace
demote_external :=
  μ(e,s,a)_notation
  + dense_glyphs
  + math_costume_without_calculation
  + repeated_campfire_cosmology
```

Keep internally if useful:

```trace
internal_surface may_use_glyphs_if_they_reduce_drift
```

Falsifier:

```trace
if outsiders need private symbolic history to use TRACE:
  public_transfer_surface := failed
```

---

## H11. External challenge surface is the non-funded route to external contact

Hypothesis:

```trace
external_challenge_surface :=
  low_cost_substitute_for_institutional_review_channel
```

Must accept:

```trace
pass
partial_pass
fail
COMPRESSION_ONLY
REGRESSION
INCONCLUSIVE
```

Guard:

```trace
author_curated_success_only := invalid_surface
```

Falsifier:

```trace
if failures_are_not_easy_to_submit_or_visible_in_ledger:
  challenge_surface := theatre
```

---

## H12. The 1% path is legitimate only if it stays demotable

Hypothesis:

```trace
one_percent_path :=
  worth_building_large
  only_if:
    every_major_claim_has_status
    + falsifier
    + demotion_path
    + failure_ledger
```

Danger:

```trace
one_percent_chance := can_launder_ambition_into_permission
```

Guard:

```trace
ambition_with_teeth := continue
ambition_without_teeth := stop_or_demote
```

---

## Current strategic verdict

```trace
verdict := continue_large_with_controls
validation := false
decision_advantage := unproven
format_transfer := shown_once_self_reported_partial
next_required := blind_regrade + negative_control + control_index + validator + open_challenge_surface
```

---

## Memory compression

```trace
remember :=
  resource_context_changes_yield_not_truth
  + weird_method_may_be_rational_under_constraint
  + internal_yield_real_but_external_yield_limited
  + build_large_with_teeth
  + cold_format_transfer_not_decision_advantage
  + controls_are_load_bearing_but_not_proof
  + DirtyDelayReceipt_first_high_bite_operator_candidate
  + EvidenceStatus_and_DirectAffectedScopeWitness_constrain_harm
  + negative_controls_required
  + repo_fork_drift_risk
  + public_surface_plain_roles_not_glyph_density
  + external_challenge_surface_needed
  + one_percent_path_must_stay_demotable
```
