# TRACE Scale With Teeth Program v0.1

Status: strategic build program. Not validation. Not canon. Not public success claim.

Purpose: preserve the large ambition of TRACE/ME while making every unproven part visible, testable, and hard to launder.

```trace
program :=
  build_large
  + label_evidence
  + attach_falsifiers
  + preserve_failure_ledger
  + expose_external_challenge_surface
  + include_negative_controls
  - validation_theatre
  - shrink_to_harmless_tests_only
```

---

## 1. Resource-adjusted frame

TRACE/ME is not being built inside a funded lab, university department, or formal research team.

```trace
resource_state :=
  one_person
  + no_institution
  + no_budget
  + no_lab
  + no_formal_review_channel
  + AI_tabs
  + GitHub
  + public_sources
  + evening_weekend_work
```

Resource context affects the yield standard.

Resource context does not affect the truth standard.

```trace
resource_context → yield_standard_adjustment
resource_context ↛ truth_standard_relaxation
```

Forbidden laundering:

```trace
one_person_no_budget ≠ validation
unusually_disciplined ≠ true
controls_generated ≠ framework_valid
```

---

## 2. Current defensible status

```trace
TRACE_current :=
  candidate_middle_out_transfer_architecture
  for harm / authority / timing / correction visibility
  under uncertainty
```

Evidence state:

```trace
evidence :=
  public_transfer_packet_built
  + schemas_and_case_graphs_built
  + one_self_reported_cold_format_transfer_run
  + documented_partial_pass
  + decision_advantage_unproven
  + repeated_falsifier_design
```

Correct label:

```trace
cold_format_transfer_self_reported_once := true
decision_advantage_over_competent_baseline := unproven
validation := false
```

---

## 3. Do not go smaller by default

A narrow falsifier is a guardrail, not the road.

```trace
blind_regrade := useful_guardrail
not := total_strategy
```

The strategic aim remains scale:

```trace
scale_goal :=
  external_use_by_AI_and_humans
  + case_graph_corpus
  + negative_controls
  + open_challenge_surface
  + reproducible_failure_ledger
  + evidence-pinned exemplars
```

Do not shrink the work to what is already proven.

Do build the large version so that the unproven parts are exposed and testable.

---

## 4. Load-bearing components

Load-bearing means: constrains error, transfers across cases, or forces a real decision/failure boundary.

```trace
load_bearing :=
  baseline_required
  + EvidenceStatus
  + DirectAffectedScopeWitness
  + DirtyDelayReceipt
  + claim_typing
  + clock_deadline_separation
  + laundering_scan
  + correction_path_realism
  + residue_ledger
  + blind_decision_delta_rubric
  + negative_controls
  + external_challenge_surface
```

Strongest current operator candidate:

```trace
lead_operator := DirtyDelayReceipt
```

Reason:

```trace
DirtyDelayReceipt :=
  separates necessary delay
  from clean resolution
  while keeping delay burden on the ledger
```

---

## 5. Decorative or externally costly components

Decorative means: may help internal compression but creates myth, friction, false precision, or external misunderstanding.

```trace
decorative_or_costly :=
  μ(e,s,a)_symbol
  + math_costume_without_calculation
  + repeated_campfire_cosmology
  + public_glyph_density
  + exact_layer_counts
  + aesthetic_self-confirmation
```

Rule:

```trace
internal_surface may_use_glyphs_if_useful
external_surface := plain_roles + examples + tests
```

Specific notation decision:

```trace
kill_symbol := μ(e,s,a)
keep_concept := entity_as_role_vector_not_moral_noun
```

---

## 6. Evidence ladder

```trace
E0 := internal_schema_or_prompt
E1 := worked_case_graph
E2 := warm_self_run
E3 := self_reported_cold_format_transfer
E4 := blind_regrade_decision_delta_signal
E5 := counterbalanced_independent_run
E6 := evidence_pinned_case_with_witness
E7 := external_human_or_AI_use_without_author_guidance
E8 := repeated_cross_domain_decision_delta_signal
```

Current Grenfell status:

```trace
Grenfell := E3_partial
```

Target next status:

```trace
Grenfell_next := E4_blind_regrade
```

---

## 7. Failure ledger as credibility infrastructure

TRACE/ME becomes more credible if failures are visible and preserved.

```trace
failure_ledger :=
  rejected_claims
  + failed_transfer_runs
  + COMPRESSION_ONLY findings
  + notation_failures
  + baseline_outperformance
  + evidence_gaps
  + survivor_witness_absence
```

Rule:

```trace
failure_count_visible := credibility_plus
failure_hidden := laundering
```

---

## 8. Negative-control corpus

The corpus must include cases where TRACE is predicted to add little or nothing.

```trace
negative_controls_required := true
```

Purpose:

```trace
negative_control :=
  prevent_universal_frame_capture
  + test_if_TRACE_only_relabels_everything
  + make_COMPRESSION_ONLY_visible
```

Pre-register per case:

```trace
case_prediction :=
  TRACE_adds_decision_delta
  OR TRACE_adds_format_only
  OR baseline_expected_to_match_TRACE
```

---

## 9. External challenge surface

Build a standing surface where strangers or AIs can submit a case, run, failure, or objection.

```trace
external_challenge_surface :=
  input_case
  + baseline_output
  + TRACE_output
  + blind_grader_result
  + failure_or_signal
  + ledger_entry
```

Must accept:

```trace
pass
partial_pass
fail
COMPRESSION_ONLY
REGRESSION
INCONCLUSIVE
```

Guard:

```trace
author_curated_success_only := invalid_surface
```

---

## 10. Large build sequence

```trace
phase_1 := blind_regrade_existing_Grenfell
phase_2 := negative_control_case_graph_corpus_5_to_10_cases
phase_3 := open_challenge_template_and_failure_ledger
phase_4 := evidence_pinned_Grenfell_with_direct_witness
phase_5 := counterbalanced_cross_model_runs
phase_6 := public_plain_language_front_surface
phase_7 := external human / AI use without author guidance
```

This is not smaller. It is larger with teeth.

---

## 11. Forbidden claims

```trace
forbidden :=
  validation
  + proven_alignment_solution
  + cold_transfer_proven
  + decision_advantage_demonstrated
  + AI_agreement_as_validation
  + controls_generated_implies_framework_valid
  + one_case_generalisation
  + self_scored_20_24_as_external_score
```

Allowed claim:

```trace
allowed :=
  TRACE/ME has produced an unusually disciplined candidate transfer architecture
  with internal controls, early cold format-transfer signal, and live falsifier path
```

---

## 12. Governing sentence

We are not shrinking the work to what is already proven. We are building the large version in a way that makes its unproven parts visible, testable, and hard to launder.
