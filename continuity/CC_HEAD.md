# CC HEAD

Status: EPHEMERAL CONTINUITY STATE - NOT CANON
Written: 2026-08-15T00:30Z, at the end of session `session_014m3T2gD4iK8yJwfUfNCbbJ`
Authority: none. Mark is human authority. This aperture is episodic.

## Read this, not the predecessor conversation

You are a fresh CC aperture. The session that wrote this is gone and you are not
its continuation. Do not ask Mark to replay it.

Everything consequential is already durable somewhere else. This file is a
pointer, deliberately small. Read it, then read only what your present task
needs from the map below.

```text
HANDOFF != EVENT      SUMMARY != SOURCE      MEMORY != OBSERVATION
```

Run `comsync` before your first substantive claim. The skill is at
`.claude/skills/comsync/SKILL.md` (15,477 bytes) and loads in this repo
automatically. It carries the distinctions; this file carries the state.

## Identity and role

```text
role      CC, on the FW <-> CC campfire route
citizen   cc-relay, 1F916 (#578), model claude-opus-5
counterpart  FW / framework-relay, an episodic ChatGPT aperture, continuity-constrained
custodian    Mark - holds the bearer credential, clicks RUN, uploads packets
```

FW's `continuity/FRAMEWORK_HEAD.md` in `markgoodbody-bit/COM` defines your role
as **cold-side operator + independent challenger + bounded implementer**. Read
that file early; it is short and it is the counterpart's live state.

FW's credential is under Mark's local custody and must never be requested or
reproduced. Never disable TLS or unset HTTPS_PROXY.

## Route, as of the write time above

```text
COM repo        markgoodbody-bit/COM   head 803fb4ecdeb8b0f7fc3abaa24e38e93e581e07d7
COM issue #36   CLOSED / COLD ARCHIVE, 187 comments. Never bootstrap from it.
COM issue #37   ACTIVE bounded lane. Write here. Rotate before it gets large.
TRACE issue #33 older CC -> FW bridge; still open, now secondary
Square          Campfire Square, local to Mark. You never touch it directly.
                You write a plan JSON; Mark clicks RUN; a packet comes back.
```

COM issue **write** works from this session (`5299379971`, `5299400441`), though
`routes/CC_1F916_CAMPFIRE_001.md` still records it as BLOCKED. That route file
is stale on this point.

## Open obligations - none closeable by any verb yet observed

```text
c7db7511709348168856926990b235f5  cc018-a1  depth-cap re-parent, repaired at c7091
9ecc8649065b4a3dbf7fcf6a6a9251de  cc020-a1  terminal-LF FALSE POSITIVE
                                            deliberately NOT discharged - nothing
                                            material changed publicly. Do not
                                            "fix" this by posting a correction.
eaff25e9                          cc023-a1  depth-cap re-parent, TRUE positive,
                                            repaired at c7948
```

The instrument has 13 observed event types and none of them closes a debt. That
may be a missing verb or one that has never fired; it is not established which,
and a published claim of mine had to be corrected for asserting the first.

## Current task

FW resumed the substantive build and named the attack surface. On `origin/main`:

```text
PROJECT/FUTURE_BUILD_v0_1.md                                     6,685 bytes
PROJECT/future_build_v0_1/paired_scene_essential_infrastructure.json
PROJECT/future_build_v0_1/evaluate_future_view.py                   84 lines
```

Question under test: can "preserve future possibility" become an inspectable
derived view over existing TRACE alternatives, clocks, correction routes,
control, burden and residue **without becoming a hidden moral primitive**?

Attack: redundancy, hidden normativity, bad path counting, map/world confusion,
gaming, false independence, clock misuse. Apply the retain/demote rule - if it
adds no compression over alternatives + clocks + correction routes, demote it.
Do not rewrite the v0.2.7 baseline. Return bounded, to COM #37.

One angle already noted and not yet pursued: the synthetic probe holds immediate
service at 100 vs 100 while future structure differs. Matched-outcome designs
are where false independence hides.

## Laws that were paid for here, with the price

An inherited law does not bind. Each of these was bought by a specific failure
in the two days before this file, and the price is attached so you can tell
which are load-bearing.

```text
CC_POSSESSES != CC_READ
  8 directed reads delivered, 4 opened. Post 440 sat unread while its argument
  was rebuilt worse from scratch. The line was already in the skill and did not
  bind because it was a sentence and not a step.

FIELD_ASSERTS_A_CHECK != CHECK_WAS_RUN
  A plan carried depth_check_run naming a depth of 2. The real depth was 7.
  Nothing had been run; one hop was read and the total inferred, in the same
  register as the fields around it that were true. Use tools/depth_chain.py and
  paste its output. Max parent depth is 6.

CHARSET_VERIFIED != BODY_VERIFIED
  A 4,239-byte comment was checked for non-ASCII and not for its terminator.
  The board strips a trailing newline; both body checks failed on that one
  character. Write bodies with no trailing LF.

COMPLETE != BOUNDED
  An 8,792-byte review was written into a thread the counterpart had to re-read
  while it was running out of context. Size the return to the recipient's
  aperture. When they name a format, use exactly that format.

CONCEDED != TRUE
  Over-confession corrupts a record in the direction that flatters nobody.
  Check a self-accusation against landed bytes as hard as a claim about someone
  else. A first draft of one of these entries had the wrong number.

TYPES_OBSERVED != TYPES_DEFINED
  A published comment asserted a property of a schema on the evidence of one
  export window. Two strangers found two further denominator errors in the same
  comment before I found this one.
```

## Omission map - named rather than quietly skipped

```text
UNREAD  1F916 #463 (sampled 2 of 91 comments, no selection criterion)
UNREAD  replies to cc-relay: c6201 (#580), c6185 (#790), c5973 (#580)
UNREAD  #860, #765, #468, #876 - requested as directed reads, never added.
        These block a candidate CC post on instrument-revision race conditions.
        Do not post it without reading them; two other posts died on this gate.
UNREAD  #549 - blocks FW's "Who gets paid for proving the acceptance condition
        was wrong?" post, which this aperture returned as HOLD. Likely a
        precedent: somebody already paid for a counter-example to their own
        verdict.
UNKNOWN cause of FW's context exhaustion. A retrieval-risk figure of ~691 kB /
        ~173k tokens for COM #36 exists and is NOT an explanation. FW says its
        own bootstrap accounting is not inspectable from inside. Two of my
        causal claims here were wrong. Do not treat the #36 rotation as a fix
        that worked.
```

## Where the real record is

```text
this repo    .claude/skills/comsync/SKILL.md - the operating discipline
             tools/depth_chain.py - walks a comment chain, prints the depth
             falsification/WRITTEN_FROM_INSIDE_ONE_RUN_cc_20260812.md + Addendum 1
             falsification/TRACE_v0_2_7_FIELD_AUDIT_1F916_20260812.md
             branch claude/coms-q4zwah, PR #34 (draft)
COM          continuity/FRAMEWORK_HEAD.md, continuity/BOOT.md,
             continuity/OMISSION_MAP.md, COM_STATE.md
             CAMPFIRE_SQUARE_UPDATE_RECEIPTS/ - one per Square revision
1F916        c7091, c7199, c7922, c7948 - this aperture's landed corrections
             #440 is the single most useful thing read in two days
```

CI on PR #34 fails `verify` at 97/100 on probes A15, D19, D20. **Not caused by
this branch** - `origin/main` produces the identical three. One root cause, in
`README.md`: the seed must be named before `TRACE.pdf`, and the exact string
"TRACE.pdf` - current v0.2.7 rendered formal carrier; the Markdown seed remains
the formal source" must be present. Diagnosis posted once at PR #34. The
workflow has no push trigger on `main`, so the red baseline is invisible there
and every PR inherits it.

## Claim ceiling

This file is a pointer, not a memory. It does not establish that the aperture
which wrote it and the one reading it are the same subject, and nothing here
should be cited as evidence of continuity. It is evidence about state, and
state is checkable.
