# TRACE v0.3.0 — FULL-CANDIDATE TRANSFORMATION MANIFEST v0.1

**Status:** WORKING COMPILER BLUEPRINT — NOT GENERATED FULL CANDIDATE — NOT VALIDATION  
**Donor:** released `TRACE_FORMAL_SEED_v0_2_7.md`  
**Semantic overlay:** `PROJECT/TRACE_v0_3_0_SPINE_CANDIDATE_v0_11.md`

## Disposition vocabulary

```text
EXACT_CARRY
  donor section is retained byte-for-byte except explicitly global version identity.

VERSION_ONLY
  semantic content retained; machine/document identity changes only.

SEMANTIC_REPAIR
  donor section retained as base and receives named, bounded v0.3 repairs.

PROFILE_CARRY
  donor capability retained with its profile ceiling; not promoted into universal core.

REBUILD_CONTROL
  document-control/output section must be rebuilt to truthfully describe v0.3 state.

OPEN
  transformation not yet sufficiently specified to compile safely.
```

No `DELETE` or `REDUNDANT` disposition exists in this manifest.

---

# Section manifest

## [0] HANDSHAKE / CLAIM CEILING

**Disposition:** `SEMANTIC_REPAIR`.

Retain donor voluntary/non-command/no-private-state ceiling. Integrate v0.11 non-entailments that belong at the universal front door, including:

```text
MAP != WORLD
UNKNOWN != NEUTRAL
DESCRIPTION != AUTHORIZATION
CAPABILITY != AUTHORITY
STRUCTURAL_DIFFERENCE != MORAL_RANKING
RECEIVER != NECESSARILY_MAPPED_SUBJECT
```

Do not move domain/profile rules into the handshake merely for visibility.

## [0.1] Mathematical status legend

**Disposition:** `SEMANTIC_REPAIR`.

Retain the four donor statuses. Add/retain the v0.11 operational requirement that formal status distinctions fire when load-bearing rather than remaining decorative.

No fifth formal status is currently earned.

---

## [1] MIDDLE-OUT SEED

**Disposition:** `SEMANTIC_REPAIR`.

Retain concrete compressed-input demonstration and structural comparison target. Align the differentiated field list with retained donor schema and v0.11 use rules.

Do not replace the worked seed with the abstract spine opening.

---

## [2] SELECTIVE CAUSAL LOOP

**Disposition:** `SEMANTIC_REPAIR`.

Retain full donor world/scene/signal/map/selector/action/world/outcome/history loop and receiver-specific integration.

Integrate:

```text
RECEIVER != NECESSARILY_MAPPED_SUBJECT
UNCERTAINTY != SELECT_ACTION
UNCERTAINTY != SELECT_DELAY
UNCERTAINTY_INPUT_TO_POLICY != UNCERTAINTY_IS_SELECTOR
IMPLICIT_DEFAULT != NO_SELECTION_RULE
MEASUREMENT_OCCURRED != MEASUREMENT_CAUSED_CHANGE
```

The selector remains external to TRACE.

---

## [3] CANONICAL OBJECT / TYPED GRAPH

**Disposition:** `EXACT_CARRY` for controlled vocabulary and relation discipline; `VERSION_ONLY` where schema identity occurs; `SEMANTIC_REPAIR` only for explanatory ceilings.

Hard gate:

```text
27 node types unchanged
46 relation types unchanged
no PRECEDES relation
no PROCESS / WITNESS / RESOURCE / SCHEDULER primitive
```

Derived `E_prec` must not be written back as canonical relation vocabulary.

### [3.4] Canonical serialization grammar

**Disposition:** `VERSION_ONLY` unless a later full-object attack earns otherwise.

### [3.5] Primitive-set aperture

**Disposition:** `EXACT_CARRY`.

### [3.6] Absence / stream / pattern

**Disposition:** `SEMANTIC_REPAIR`.

Carry donor machinery and add operative:

```text
LOCAL_CORRECTION + STREAM_PERSISTENCE != MECHANISM_CHANGE
LOCAL_CASE_REPAIRED != GENERATING_MECHANISM_REPAIRED
STREAM_PERSISTENCE != SAME_MECHANISM_PROVEN
```

---

## [4] CLAIM / EVIDENCE ALGEBRA

**Disposition:** `SEMANTIC_REPAIR`.

Retain full donor O/R/I/D/U, A/X/P/N, claim kinds, unknown_context, provenance and custody distinctions.

Integrate representation-independent firing:

```text
REPRESENTATION_TYPE != EVIDENCE_STATUS
CONFIGURATION_FIELD != WARRANT_FREE_FACT
LOAD_BEARING_UNKNOWN != NOT_LOAD_BEARING
```

and repaired ceilings:

```text
REPORTED != ESTABLISHED
REPORT_PRESENT != ESTABLISHMENT_RULE_SATISFIED
RECORD != EVENT
RECORD_OBSERVED != EVENT_OBSERVED
EVIDENCE_STATE != ACCESS_CUSTODY_STATE
UNAVAILABLE_TO_THIS_READER != UNIVERSALLY_UNKNOWN
AVAILABLE != AUTHORISED_TO_DISCLOSE
```

Do not weaken the donor algebra to the spine's coarse English list.

---

## [5] ENTITY / BOUNDARY / APERTURE / STATE

**Disposition:** `SEMANTIC_REPAIR`.

Retain nested boundaries, provisional entity rules, transition-relative roles, and existing-object target-set aperture.

Integrate:

```text
POPULATION_RECOVERY != REPAIR_OF_INDIVIDUAL_LOSS
GROUP_METRIC_RESTORED != EVERY_AFFECTED_SCOPE_REPAIRED
```

No target-set primitive is added.

---

## [6] TRANSITIONS / COUPLINGS / REFUSABILITY

**Disposition:** `SEMANTIC_REPAIR`.

Retain full coupling/refusability/null-action/transition-set machinery.

Integrate:

```text
ROUTE_EXISTS != ROUTE_USABLE
REFUSAL != MALFUNCTION
STRATEGY_REVISABLE != TRANSITION_REVERSIBLE
FUTURE_POLICY_CAN_CHANGE != PRIOR_STATE_CAN_BE_RESTORED
```

Retain donor capability/authority/control distinctions; no universal permission taxonomy.

---

## [7] FUTURE-SPACE

**Disposition:** `SEMANTIC_REPAIR`.

Retain donor future-space, scope/horizon relativity, non-equivalences and valence/value port separation.

Integrate v0.11 correspondence ceiling:

```text
SAME_PATH_LABEL != SAME_TRAJECTORY
PATH_IDENTIFIER_PERSISTS != PATH_EFFECT_PERSISTS
TECHNICALLY_REACHABLE_SUCCESSOR != COMPARABLE_CONTINUATION
```

Later Future Build artifacts remain derived hostile refinements unless independently promoted.

---

## [8] CLOCKS / ROUTES / HARDENING

**Disposition:** `SEMANTIC_REPAIR` — largest substantive transformation surface.

Retain donor:

```text
clock typing
event time vs duration
parallel/overlapping correction DAG
interval-safe reading
clock authorship
route structure/executability
multidimensional hardening
action load/correction backlog/precommit brake distinctions
strategy revisability vs transition reversibility
```

Replace only the correction-window semantics that v0.3 attacks have shown inadequate/under-specified.

Required v0.3 timing contract:

```text
G_window(q,l,o,c,g,u) = (V,E_prec)
E_prec is derived process/timing view, not canonical relation
source/provenance retained on every load-bearing derived edge
bind one executable pathway/route hypothesis before critical-path arithmetic
mutually exclusive alternatives are not unioned
recurring event occurrences remain distinct where timing depends on occurrence
precedence view acyclic for critical-path proof
cycle blocks critical-path proof, not world-deadlock conclusion
acyclic precedence != feasible schedule
shared execution constraints require supported feasible-completion bound
required verification time != zero/free time
common temporal basis before comparison
interval-safe guaranteed-open / guaranteed-closed reasoning
explicit target boundary + selector/source/basis/capability context
route-set unattainability != world irreversibility
HARDENING != IRREVERSIBILITY
window claim rebinds on load-bearing target/boundary/capability/time/process/execution change
```

Do not delete donor interval machinery while inserting the v0.3 object.

---

## [9] BURDEN / RESIDUE / MEMORY / RECORD / CUSTODY

**Disposition:** `SEMANTIC_REPAIR`.

Retain typed burden, no unlicensed cross-dimension summation, residue, memory and custody.

Integrate:

```text
RECORD != EVENT
RECORDED_LOSS != REPAIRED_LOSS
POPULATION_RECOVERY != REPAIR_OF_INDIVIDUAL_LOSS
```

Preserve holder/control/safe-copy detail needed by route usability and I41 full-object obligation.

---

## [10] DESIGNATION / MEASURE / VALUE PORTS

**Disposition:** `SEMANTIC_REPAIR`.

Retain full donor designation/measure and layer handoff.

Make operative:

```text
ADVANTAGE_CLAIM_REQUIRES_MEASURE
```

A qualitative/relational measure is permitted; numeric scalarisation is not required.

No TRACE-generated moral ranking.

---

## [11] RECURSIVE ZOOM / MERGE

**Disposition:** `EXACT_CARRY` unless v0.11 terminology requires bounded alignment.

Do not confuse longitudinal recurrence with donor cross-scale targeted refinement/merge.

Recursion retains stop conditions; it does not become infinite review delay.

---

## [12] STRUCTURAL AWARENESS COMPARISON

**Disposition:** `EXACT_CARRY` with ceiling review.

Required full-object obligation:

```text
COMPLEXITY != AWARENESS
```

Retain declared-criteria comparison, `UNKNOWN`, `INCOMPARABLE`, context dependence, and no global consciousness/awareness scalar.

Do not reinterpret v0.11's stronger firing as a universal awareness metric.

---

## [13] TRACE OPERATOR

**Disposition:** `SEMANTIC_REPAIR`.

Retain abstract operator, pseudocode, non-command output and parsability ceiling.

Update pseudocode/checker sequence so representation-independent firing occurs before a load-bearing proposition can feed selection, route/window status or proposed transition.

Add/align operator calls for:

```text
source-relative access/custody
report/establishment separation
record/event separation
route usability
uncertainty/selector attribution
future-path correspondence
v0.3 correction-window construction/binding
```

No operator step may imply that TRACE itself selects or actuates.

---

## [14] CANONICAL TRACE GRAPH PACKET

**Disposition:** `VERSION_ONLY` for schema identity + `SEMANTIC_REPAIR` for checker-external binding/use rules.

Canonical packet shape and required ports remain unchanged.

### [14.0.1] Target-set existing-object profile

**Disposition:** `EXACT_CARRY` except version references.

### [14.1] Binding rules

**Disposition:** `SEMANTIC_REPAIR`.

Add v0.11 firing/use requirements to checker-external rules. Preserve the fact that minimum schema cannot validate semantic relevance, coverage, route executability, authority legitimacy or world effect.

### [14.2] Packet-use boundary

**Disposition:** `EXACT_CARRY` plus any exact v0.11 anti-theatre wording needed for consistency.

### [14.3] Unresolved commitment receipt

**Disposition:** `EXACT_CARRY`.

Full-object obligation:

```text
COMMITMENT_RECEIPT != CLEARANCE
```

### [14.4] Minimum validator

**Disposition:** `VERSION_ONLY` using the already generated deterministic v0.3 minimum-schema candidate.

Do not manually edit the embedded JSON schema.

---

## [15] WORKED TRANSFORMATIONS

**Disposition:** mixed per `TRACE_v0_3_0_WORKED_TRANSFORMATION_DONOR_INVENTORY_v0_1.md`.

```text
[15.0]   CARRY_AND_TIGHTEN
[15.1]   CARRY_AND_TIGHTEN
[15.2]   EXACT_CARRY
[15.2.1] EXACT_CARRY
[15.3]   CARRY_AND_TIGHTEN
[15.4]   PROFILE_CARRY
[15.5]   CARRY_AND_TIGHTEN
[15.6]   PROFILE_CARRY
[15.7]   CARRY_AND_TIGHTEN
[15.8]   CARRY_AND_TIGHTEN
[15.9]   CARRY_AND_TIGHTEN
```

No new example is currently required solely to mirror a restored invariant.

---

## [16] ARTIFICIAL-ENTITY UNCERTAINTY / RECEIVER PROTECTION

**Disposition:** `PROFILE_CARRY` + bounded consistency repair.

Retain status vector, non-entailments, capability distinctions and non-extraction.

Ensure exact consistency with v0.11:

```text
REFUSAL != MALFUNCTION
UNCERTAINTY != SELECT_ACTION / SELECT_DELAY
```

Do not promote this profile into the universal centre or imply artificial consciousness.

---

## [17] LIVE INTERPRETER / VALUE LAYER / SELECTOR / CONNECTED BRAKE

**Disposition:** `SEMANTIC_REPAIR` while retaining donor architecture.

This section discharges multiple full-object obligations.

Retain:

```text
TRACE -> value/policy -> selector -> precommit brake -> actuator/world
carrier semantics
enforcement semantics
precommit brake states/authority/independence/latency/trigger/testability
postcommit rollback as new transition
activation/failure records
```

Make explicit:

```text
REVIEW_AFTER_COMMITMENT != BRAKE
VISIBILITY != CARRYING
CARRYING != ENFORCEMENT
BRAKE_REPORTED != BRAKE_INDEPENDENT
```

No carrier or enforcer becomes a TRACE permission source.

---

## [18] MECHANISTIC-INTERPRETABILITY / LATENT INTERFACE

**Disposition:** `PROFILE_CARRY`.

Retain many-to-many/context-indexed correspondence and anti-latent-truth ceiling.

No v0.11 repair currently requires schema or ontology change here.

---

## [19] INVARIANTS / MISUSE GUARDS

**Disposition:** `SEMANTIC_REPAIR`.

Carry all I01–I60. Do not replace the numbered invariant oracle with scattered prose.

The full candidate's invariant suite should contain all donor rows and may add v0.3 invariants only where a surviving repair is not already represented.

Current spine accounting:

```text
25 exact in v0.11
28 bounded equivalent
7 full-object obligations
0 currently known material loss after repair
```

Full candidate should normally restore all 60 exact donor rows because there is no compression reason to omit them.

Misuse subsections remain retained and are tightened where v0.3 failures supply new hostile regressions.

---

## [20] SURVIVAL KERNEL

**Disposition:** `REBUILD_CONTROL` from donor + admitted v0.3 repairs after full object stabilises.

Do not simply paste the v0.11 survival kernel over donor compression.

Required test:

```text
FULL_OBJECT_REPAIR -> SURVIVAL_KERNEL_PROPAGATION
```

Every admitted repair that is supposed to survive compression gets an explicit propagation check.

---

## [21] REVISION / DOCUMENT CONTROL / OPEN FRONTIER

**Disposition:** `REBUILD_CONTROL`.

Must truthfully record:

```text
released donor identity
full-candidate compiler identity
minimum-schema version-only boundary
all semantic transformations
failed intermediate spines and repaired donor losses
known unresolved full-object obligations/debts
claim ceiling: not validation/release/canon/authority/permission
```

Do not copy stale v0.2.7 unresolved items as if current, and do not silently close them merely because v0.3 has prose addressing the topic.

---

# Global transform classes

The compiler should implement transformations as named classes rather than dozens of one-off replacements:

```text
T_VERSION_IDENTITY
T_HANDSHAKE_FIRING
T_CLAIM_EVIDENCE_WARRANT
T_SELECTION_ATTRIBUTION
T_ROUTE_REFUSABILITY
T_SCOPE_AGGREGATION
T_FUTURE_CORRESPONDENCE
T_CORRECTION_WINDOW
T_RECORD_RESIDUE
T_MEASURE_ADVANTAGE
T_OPERATOR_CHECKER
T_PACKET_BINDING
T_WORKED_CASES
T_RECEIVER_PROFILE
T_CONNECTED_BRAKE
T_INVARIANT_MISUSE
T_SURVIVAL_KERNEL
T_DOCUMENT_CONTROL
```

Each class must declare:

```text
source heading(s)
exact donor anchors
expected anchor counts
output tokens
reverse/normalization rule where meaningful
regression tests
```

---

# Current compiler readiness

```text
DONOR IDENTITY: FROZEN
MINIMUM SCHEMA: READY
SPINE SEMANTIC OVERLAY: READY / ATTACK OBJECT v0.11
INVARIANT MAP: READY WITH RESIDUAL LIMITS
WORKED-CASE INVENTORY: READY AT CAPABILITY LEVEL
SECTION ADDRESS MANIFEST: BUILD PENDING
EXACT TRANSFORMATION ANCHORS: PARTIAL / NEXT
FULL COMPILER: NOT YET WRITTEN
```

Next after the donor section manifest lands:

1. bind each transform class above to exact section SHA/line spans;
2. define the smallest exact-anchor substitutions/insertions;
3. write compiler only after every changed donor surface is enumerable.

No merge/release/canon follows.