# TRACE v0.3.0 — INTEGRATION AND DELETION PASS v0.1

**Status:** WORKING SELECTION / HISTORY LEDGER — NOT FORMAL BASELINE — NOT CANON — NOT VALIDATED  
**Current-state refresh:** 2026-08-25  
**Purpose:** decide what recent TRACE/field work earns a place in the universal centre, what remains derived/profile/tooling, and what should be deleted or held rather than accumulated.

## 0. Current-state correction

Earlier revisions of this ledger described spine v0.6 as current and full-candidate construction as a future gate. Those statements are preserved in Git history as accurate descriptions of their time but are **not current now**.

Current working objects are:

```text
spine:
  PROJECT/TRACE_v0_3_0_SPINE_CANDIDATE_v0_11.md
  semantic commit 41fafe81a681cdc6514efc13524bae6ea6d6af8d

full candidate:
  PROJECT/TRACE_FORMAL_SEED_v0_3_0_FULL_WORKING_CANDIDATE_v0_1.md
  working / attack object

project gate:
  PROJECT/TRACE_v0_3_0_EVALUATION_FREEZE_v0_1.md
```

F03-F10 have been preserved as coherence findings and repaired at source-contract level. Recursion and F10 carrier survival are bounded-closed for the current pass. No new primitive/root or minimum-schema expansion was earned.

The present selection decision is therefore **not more integration by momentum**. Semantic expansion is frozen for one outward-evaluation cycle.

```text
RECORDED_CURRENT != CURRENT_NOW
FINDING_FOUND != REPAIR_REQUIRED
REPAIR_POSSIBLE != CONTINUATION_JUSTIFIED
```

---

## 1. Governing bias

```text
LESS ONTOLOGY -> MORE CAUSAL DISCIPLINE
FIELD_FINDING != NEW_PRIMITIVE
USEFUL_TOOL != CORE_GRAMMAR
FAILED_OBJECT != ERASED_OBJECT
DONOR_RECOVERY != NEW_INVENTION
INTEGRATED != COMPRESSED
```

A new object is not promoted merely because it is useful, elegant, independently rediscovered or successful on one case.

---

## 2. Current dispositions

```text
NEW PRIMITIVE: NO
NEW ROOT:      NO

DEPENDENCY-PATH OPERATOR
  -> DERIVED / HOSTILE TRIAL

COLD-ENTRY SURFACE
  -> PROFILE / COLD TRIAL

CORRECTION PREFLIGHT
  -> CHECKER-EXTERNAL / HOLD

FIELD INSTRUMENTS
  -> TOOLING / EVIDENCE

SEMANTIC EXPANSION
  -> FROZEN FOR ONE OUTWARD-EVALUATION CYCLE
```

No current evidence requires these held objects to become universal TRACE primitives.

---

## 3. Field-earned repairs retained in the current line

Recent field/donor work earned distinctions or activation rules rather than new ontology:

### 3.1 Measurement can alter the measured field

```text
MEASUREMENT != PASSIVE_OBSERVATION
MEASUREMENT_OCCURRED != MEASUREMENT_CAUSED_CHANGE
```

### 3.2 Freshness can expire through relevant dependency change

```text
DATE_CURRENT != DERIVED_VALUE_CURRENT
SOURCE_MUTATED != LOAD_BEARING_DEPENDENCY_CHANGED
MUTATION_OBSERVED != CLAIM_INVALIDATED
```

### 3.3 Verification must discriminate the failure that matters

```text
CHECK_EXISTS != CHECK_EXECUTED
CHECK_EXECUTED != CHECK_DETECTS_TARGET_FAILURE
STATIC_CORRECTNESS != OPERATIONAL_DISCRIMINATION
```

### 3.4 Liveness loss does not diagnose cause

```text
SILENCE != TAMPERING
PROCESS_EXISTS != PROCESS_HEALTHY
SAFE_EXCLUSION != LIVENESS
```

### 3.5 Generic firing is representation-independent

```text
DISTINCTION_PRESENT != DISTINCTION_APPLIED
REPRESENTATION_TYPE != EVIDENCE_STATUS
CONFIGURATION_FIELD != WARRANT_FREE_FACT
```

### 3.6 Evidence state is separate from access/custody state

```text
EVIDENCE_STATE != ACCESS_CUSTODY_STATE
UNAVAILABLE_TO_THIS_READER != UNIVERSALLY_UNKNOWN
AVAILABLE != AUTHORISED_TO_DISCLOSE
```

### 3.7 Future-path correspondence cannot be inferred from labels

```text
SAME_PATH_LABEL != SAME_TRAJECTORY
PATH_IDENTIFIER_PERSISTS != PATH_EFFECT_PERSISTS
```

The richer machinery remains in the full candidate where required; the compact spine should not be inflated merely to repeat donor detail.

---

## 4. Correction-window integration outcome

The original serial inequality remains at most a bounded special case. The current centre uses event/precedence structure when overlap/branching matters, includes load-bearing verification, requires represented target-boundary conditions, keeps route/capability-relative unattainability separate from world irreversibility, requires temporal comparability, distinguishes structural parallelism from feasible concurrency, and preserves UNKNOWN where required support is absent.

```text
POINT_ESTIMATE_FITS != GUARANTEED_OPEN
SAME_UNIT != SAME_REFERENCE_EVENT
NO_PRECEDENCE_EDGE != CONCURRENCY_AVAILABLE
STRUCTURAL_PARALLELISM != FEASIBLE_PARALLELISM
MULTIPLE_LOAD_BEARING_BOUNDARIES != ONE_UNQUALIFIED_CLOSE
```

No scheduler/resource/capacity/target-boundary/correction-window primitive was added.

This surface is not the current quarry. Reopen only for a materially distinct counterexample.

---

## 5. Failed integration ancestry remains evidence

```text
SPINE REPAIR v0.2
  -> failed: common temporal basis + false-staleness gaps

SPINE REPAIR v0.3
  -> failed: precedence-only critical path ignored shared execution capacity

COHERENT SPINE v0.3
  -> failed integration objective through accretion

COMPRESSED SPINE v0.4
  -> failed generic firing / trigger regression

SPINE v0.5
  -> failed donor-loss pass: access/custody separation + path correspondence

SPINE v0.6
  -> survived its bounded transfer pass but was later succeeded by further repaired spine objects

SPINE v0.7-v0.11
  -> subsequent working lineage; see PR #38 and exact files for evidence

FULL CANDIDATE F01-F10
  -> source-level coherence findings preserved; recursion and F10 currently bounded-closed
```

`FAILED_OBJECT != ERASED_OBJECT`.

---

## 6. No-additions / held material

Do **not** currently promote merely by importance or availability:

- universal resource/scheduler/queue/capacity ontology;
- universal witness or liveness primitive;
- measurement-intervention primitive;
- generic process primitive;
- new target-boundary primitive;
- universal permission/authority taxonomy;
- null-result statistical rules into TRACE core;
- Campfire worker/mutex/metadata implementation details;
- all donor invariants as prose in the spine;
- full access/custody algebra into the spine;
- trajectory-matching algorithm into the spine;
- enforcement expansion from F10 carrying;
- a new coherence finding solely because the previous finding is closed.

---

## 7. Current gate: outward evaluation

Before further semantic expansion, freeze and run an evaluation capable of making TRACE shrink or lose.

Required dimensions are defined in `PROJECT/TRACE_v0_3_0_EVALUATION_FREEZE_v0_1.md` and include:

- ordinary careful-analysis comparator;
- predeclared mixed non-project-native cases;
- negative control;
- cold receiver;
- misses / false positives / over-firing;
- time/token/cognitive cost;
- end-to-end semantic survival;
- explicit shrink/demote/HOLD criteria.

Do not select favorable cases before the protocol is frozen.

```text
SPINE != FULL CANDIDATE
FULL CANDIDATE != RELEASE
COLD TRANSFER != VALIDATION
ARTIFACT_COHERENCE != PRACTICAL_ADVANTAGE
```

No merge/release/canon follows from the current build or from surviving the outward evaluation.
