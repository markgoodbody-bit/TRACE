# TRACE v0.3.0 — OUTWARD API EXECUTION AUTHORIZATION — 2026-08-29 v0.1

**Status:** HUMAN AUTHORIZATION RECORD — BOUNDED EXECUTION ONLY — NOT EFFICACY RESULT

## Authorized scope

Immediately after Codex stated the proposed boundary below, Mark replied: `ok agreed. proceed`.

> Authorize up to USD 4 total, including two preliminary connection tests and the exact 32-call
> Gemini/Kimi TRACE study.

This records the operational authority as:

```text
maximum aggregate paid-equivalent ceiling = 4.00 USD
preliminary connection tests = 2 exactly (Gemini, then Kimi)
primary calls = 32 exactly
receiver families = GEMINI_GOOGLE, KIMI_MOONSHOT
Qwen calls = 0
retries = 0
manual fallbacks = 0
```

The active candidate is
`PROJECT/TRACE_v0_3_0_OUTWARD_API_TRANSPORT_PREFLIGHT_CANDIDATE_v0_2_TWO_FAMILY.md`.
The bound manifest SHA-256 is
`5b2ea0e916409d9283991bee4e55d2ca5be5af7bea99c5801562aa5889ae1eab`.

## Execution conditions

1. Campfire v0.18.33 remains the server-owned Money Guard and dispatch aperture.
2. Gemini is aligned to exact configured model `gemini-3.6-flash` before its connection test.
3. Kimi remains `kimi-k3`.
4. Every primary call receives a fresh exact server estimate and exact fingerprint before round
   creation and one-use dispatch.
5. A local append-only ledger maintains the study-wide USD ceiling above per-call Money Guard.
6. Any connection-test failure stops before primary dispatch.
7. Any primary HTTP/terminal failure stops the run. It is preserved and is not retried.
8. Raw returns are preserved separately by exact bytes and hash.
9. No Qwen endpoint may be contacted by this run.

The 4.00 USD figure is a maximum authorized ceiling, not an instruction to spend it and not a claim
that provider billing cannot exceed a pre-run estimate.
