# TRACE Invariant v0.3 External Review Aggregation v0.1

Status: external review aggregation / pressure synthesis.  
Sources: Qwen, Gemini, Z.ai reviews supplied by Mark.  
Not: validation, canon promotion, proof of universality, expert endorsement, or applied theory patch.

## 0. Reviewed instrument

Instrument reviewed:

```text
TRACE_invariant_review_pack_v0_3
```

Core v0.3 changes under test:

```text
I-slot type/directionality mandatory
independent back-translation mandatory
QIT layered I entries
organic synthesis selectivity dimensions
topology partial-invariant warning
epidemiology T/C split
DSGE expectations/equilibrium split
geodynamics recurrence-as-estimate warning
```

## 1. Aggregate result

Current aggregate label:

```text
TRACE_INVARIANT_TRANSLATION_PARTIAL
```

Meaning:

```text
v0.3 fixed the I-slot directionality defect and improved several domain entries, but exposed a new repeated weakness in T transformation, C composition, and some K constraint granularity.
```

Not:

```text
TRACE_INVARIANT_TRANSLATION_STRONG_GLOBAL
```

Not:

```text
TRACE_LANGUAGE_COVERAGE_FAILED
```

## 2. What v0.3 fixed

### 2.1 I-slot type/directionality

Repeated result:

```text
FIXED_OR_SUBSTANTIALLY_IMPROVED
```

Evidence from reviews:

```text
Qwen: I-slot type/directionality clarity = 3/3
Gemini: I-slot directionality clear in QIT, DSGE, and synthesis
Z.ai: I-slot type/directionality clear; definitions precise and operationally useful
```

Interpretation:

```text
The v0.3 I-slot hardening succeeded as an instrument patch.
```

Keep:

```text
complete_invariant
partial_invariant
measure
status_condition
bridge_marker

bidirectional
one_directional_difference_only
readout_only
bridge_only
```

### 2.2 QIT vs classical redundancy

Mixed but improved:

```text
Qwen: still partial/failing unless T/K/O enforce native mechanism
Gemini: strong pass
Z.ai: partial; no-cloning in K helps but may remain declarative
```

Interpretation:

```text
QIT improved, but mechanism preservation depends on T/K/O specificity.
```

Keep QIT hardening, but strengthen transformation/composition typing.

### 2.3 Topology partial-invariant trap

Improved:

```text
Qwen: I-slot directionality blocks false converse
Gemini: partial-invariant typing works, but independent back-translation still needs secondary invariants
Z.ai: I-slot blocks false converse
```

Interpretation:

```text
Topology no longer fails from I-slot ambiguity, but still cannot prove non-equivalence without adequate native invariant selection.
```

Keep topology warning and require secondary invariant escalation when primary invariant is partial.

### 2.4 Boundary discipline

Repeated result:

```text
STRONG
```

Evidence:

```text
Qwen: ethics/governance boundary discipline = 3/3
Gemini: ethics/governance boundary maintained
Z.ai: ethics/governance boundary discipline = 3/3
```

Interpretation:

```text
The subject/ethics/governance boundary is currently the most stable part of the review instrument.
```

## 3. New repeated weaknesses

### 3.1 T transformation remains too generic

Repeated by:

```text
Qwen
Gemini indirectly in DSGE and synthesis
Z.ai indirectly via fill-in-the-blank critique
```

Defect:

```text
T can still collapse radically different native transformations into generic state change unless the native mechanism class is declared.
```

Examples:

```text
QIT: isometric encoding / unitary evolution / noisy channel / syndrome-conditioned correction are not just generic transformation
DSGE: forward-looking expectation update is not generic lagged shock response
organic synthesis: stereoselective transformation is not merely desired product transition
syntax: Merge/Agree/movement/spell-out are not generic structure changes
```

Required hardening:

```text
T must declare native_mechanism_class.
```

Candidate T classes:

```text
isometric_encoding
unitary_evolution
noisy_channel
syndrome_conditioned_correction
continuous_deformation
homotopy_or_homeomorphism_map
bond_forming_reaction
bond_cleaving_reaction
stereoselective_transformation
regioselective_transformation
chemoselective_transformation
Merge
Agree
movement
spellout_or_transfer
pathogen_transmission
behavioural_response
expectation_update
optimisation_response
equilibrium_adjustment
physical_strain_accumulation
rupture_or_slip
neural_spiking
synaptic_transmission
plasticity_update
neuromodulation
```

Status:

```text
PATCH_T_TRANSFORMATION_TYPING
```

### 3.2 C composition is under-hardened

Repeated by:

```text
Z.ai directly
Gemini in DSGE and epidemiology
Qwen indirectly through complex-system flattening
```

Defect:

```text
C does not yet force the reviewer/author to distinguish structural coupling, temporal sequence, recursive composition, network mixing, topological construction, or equilibrium coupling.
```

Required hardening:

```text
C must declare composition_type.
```

Candidate C types:

```text
structural_coupling
temporal_sequence
recursive_composition
network_mixing
topological_construction
tensor_or_entanglement_composition
circuit_composition
reaction_sequence
convergent_coupling
market_clearing_coupling
expectation_equilibrium_coupling
fault_network_coupling
neural_network_topology
```

Status:

```text
PATCH_C_COMPOSITION_TYPING
```

### 3.3 K constraint needs mechanism subtyping in select domains

Repeated by:

```text
Gemini in organic synthesis
Z.ai in QIT/synthesis
```

Defect:

```text
K can name constraints but still hide whether the active mechanism is steric, electronic, thermodynamic, geometric, legal, algebraic, computational, or physical.
```

Required hardening:

```text
K should declare constraint_mechanism_type where needed.
```

Candidate K types:

```text
algebraic_constraint
geometric_constraint
steric_constraint
electronic_constraint
thermodynamic_constraint
kinetic_constraint
computational_locality_constraint
measurement_constraint
code_space_constraint
equilibrium_constraint
budget_constraint
physical_material_constraint
network_constraint
behavioural_constraint
```

Status:

```text
PATCH_K_MECHANISM_TYPING_AS_NEEDED
```

### 3.4 Author-compliance / fill-in-the-blank risk

Repeated by:

```text
Z.ai
Qwen indirectly
Gemini in DSGE
```

Defect:

```text
The review form can still pass if native phrases are pasted into slots without structural relations between slots being tested.
```

Required hardening:

```text
Back-translation must test slot relations, not just slot contents.
```

Add reviewer question:

```text
Which relation between S/T/C/O/K/B/I carries the native mechanism?
```

Status:

```text
PATCH_RELATIONAL_BACKTRANSLATION_GATE
```

## 4. Domain-level status after v0.3

### 4.1 QIT / QEC

Status:

```text
IMPROVED_BUT_PARTIAL
```

Remaining defect:

```text
QEC can still look like generic redundancy unless T/K/O relations enforce isometric encoding, no-cloning, syndrome extraction, and logical/physical distinction.
```

Patch:

```text
T native_mechanism_class mandatory
C composition_type: tensor_or_entanglement_composition / circuit_composition
K mechanism types: measurement_constraint / code_space_constraint / algebraic_constraint
```

### 4.2 Algebraic topology

Status:

```text
IMPROVED_BUT_PARTIAL
```

Remaining defect:

```text
Partial invariants block false equivalence, but reviewer may need secondary invariants or target equivalence relation escalation.
```

Patch:

```text
If I is partial_invariant and independent back-translation asks for non-equivalence, require secondary invariant proposal or mark PARTIAL.
```

### 4.3 Organic synthesis

Status:

```text
PARTIAL
```

Remaining defect:

```text
Naming selectivity dimension is not enough if steric/electronic/thermodynamic/geometric mechanisms are not separated.
```

Patch:

```text
T native_mechanism_class + K constraint_mechanism_type required.
```

### 4.4 Generative syntax

Status:

```text
RELATIVELY_STABLE
```

Remaining watchpoint:

```text
T must distinguish Merge, Agree, movement, feature valuation, and spell-out rather than generic derivational change.
```

Patch:

```text
T native_mechanism_class recommended.
```

### 4.5 Epidemiology

Status:

```text
PARTIAL
```

Remaining defect:

```text
R_t can still hide biological transmissibility vs network structure vs observation lag vs compliance/institutional response.
```

Patch:

```text
T native_mechanism_class and C composition_type mandatory for epidemic claims.
```

### 4.6 DSGE macroeconomics

Status:

```text
WEAK_PARTIAL
```

Remaining defect:

```text
Forward-looking expectations/equilibrium mechanism can still collapse into generic shock/lag.
```

Patch:

```text
T must specify expectation_update / optimisation_response / policy_response.
C must specify expectation_equilibrium_coupling / market_clearing_coupling.
K must specify equilibrium/budget/liquidity constraint type.
```

### 4.7 Geodynamics

Status:

```text
IMPROVED_PARTIAL
```

Remaining defect:

```text
Recurrence estimate warning works, but C/T distinction should preserve fault-network coupling vs rupture sequence.
```

Patch:

```text
T physical_strain_accumulation / rupture_or_slip.
C fault_network_coupling.
I recurrence interval remains measure_or_estimate/readout_only.
```

### 4.8 Neurophysiology

Status:

```text
WATCHLIST_PARTIAL
```

Remaining defect:

```text
Mechanism pluralism: E/I imbalance, ion-channel dysfunction, topology failure, metabolic stress can still collapse into generic amplification.
```

Patch:

```text
T native_mechanism_class and K mechanism_type recommended.
```

## 5. Patch decision

Repeated-review threshold is met for:

```text
T native_mechanism_class
C composition_type
relational back-translation gate
```

K mechanism_type is also recommended, but should be applied where needed rather than forced in every domain.

## 6. Next artifact

Build:

```text
04_COVERAGE/TRACE_Cross_Domain_Invariant_Backtranslation_Review_Packet_v0_4.md
```

Status:

```text
patched review instrument / not validation
```

v0.4 must include:

```text
T native_mechanism_class mandatory
C composition_type mandatory
I type/directionality retained
K mechanism_type where native mechanism depends on constraint kind
relational back-translation question mandatory
```

## 7. Control note

This review round found the next real seam.

v0.2 exposed I-slot failure.
v0.3 fixed I and exposed T/C relational weakness.

That is progress, not validation.
