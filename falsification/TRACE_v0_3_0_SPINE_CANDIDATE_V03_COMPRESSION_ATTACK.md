# TRACE v0.3.0 — SPINE CANDIDATE v0.3 COMPRESSION ATTACK

**Status:** HOLD v0.3 AS INTEGRATED SPINE / RE-COMPRESS BEFORE COLD TRANSFER  
**Target:** `PROJECT/TRACE_v0_3_0_SPINE_CANDIDATE_v0_3.md`  

---

## 1. Cheap gate result

Repository object sizes at the same branch state:

```text
SPINE_CANDIDATE_v0_2 = 22,924 bytes
SPINE_CANDIDATE_v0_3 = 31,714 bytes
DELTA                 = +8,790 bytes
CHANGE                = +38.34%
```

The v0.3 object is still far smaller than the full v0.2.7 donor, but that is not the relevant local comparison. The declared integration/deletion objective was to fold only load-bearing repairs into the existing spine, not to concatenate the repair stack.

```text
INTEGRATED != COMPRESSED
REPAIR_SURVIVED != REPAIR_TEXT_ALL_REQUIRED
MORE_GUARDS != BETTER_SPINE
```

---

## 2. Where expansion occurred

The added size is concentrated in four field-earned seams plus earlier donor-loss repairs:

- measurement/reactivity;
- currentness/freshness;
- verification/liveness/witness;
- correction-window process/timing;
- capability/authority trigger and selected evidence ceilings.

The distinctions are not rejected. The question is whether the spine needs their full repair-note wording.

Several paragraphs repeat claims already carried by adjacent spine structure. Examples:

```text
full lists of possible invalidators
full lists of possible shared execution constraints
multiple equivalent timing non-entailments
repeated declarations that no new primitive is introduced
repair-history language inside the candidate header/body
profile/tooling dispositions that can remain in the build ceiling
```

---

## 3. Compression rule

For the next spine candidate:

1. keep the behavioural distinction;
2. keep one decisive counterexample-blocking guard where needed;
3. keep the minimum firing condition;
4. remove repair-history explanation from the spine itself;
5. leave detailed examples, failed cases, candidate taxonomies and implementation guidance in repair/falsification/derived files;
6. do not duplicate a donor/spine guard merely because a field case rediscovered it.

Preferred local target:

```text
SPINE_v0_4 <= approximately SPINE_v0_2 size
```

This is a pressure target, not a semantic law. A small increase is acceptable if a distinction demonstrably cannot survive compression. A 38% increase requires explicit justification and currently has none.

---

## 4. Distinctions that must survive compression

Do not delete the causal content earned by attacks:

```text
UNKNOWN != NEUTRAL
OPERATOR_TARGET_SET != AUTHORITATIVE_TARGET_SET
CONFIDENCE != TRUTH / AUTHORITY
CAPABILITY != AUTHORITY with conditional firing
MEASUREMENT_OCCURRED != MEASUREMENT_CAUSED_CHANGE
CURRENTNESS CAN EXPIRE THROUGH LOAD-BEARING DEPENDENCY CHANGE
CHECK_EXISTS != CHECK_EXECUTED != CHECK_DETECTS_TARGET_FAILURE
SILENCE != CAUSE
EXTERNAL != INDEPENDENT
TARGET BOUNDARY MUST BE EXPLICIT
SAME_UNIT != SAME_REFERENCE_EVENT
PRECEDENCE PARALLELISM != FEASIBLE PARALLELISM
GUARANTEED OPEN REQUIRES SUPPORTED FEASIBLE COMPLETION UPPER BOUND
MULTIPLE LOAD-BEARING BOUNDARIES != ONE UNQUALIFIED CLOSE
ROUTE-RELATIVE UNATTAINABILITY != WORLD IRREVERSIBILITY
```

The correction-window serial inequality may remain only as bounded shorthand.

---

## 5. Disposition

```text
SPINE_CANDIDATE_v0_3: SEMANTIC INTEGRATION OBJECT / HELD FOR COMPRESSION
COLD TRANSFER: NOT YET
NEXT: BUILD v0.4 COMPRESSION CANDIDATE
```

Do not erase v0.3. It is evidence that successful repair work can still fail the integration objective through accretion.
