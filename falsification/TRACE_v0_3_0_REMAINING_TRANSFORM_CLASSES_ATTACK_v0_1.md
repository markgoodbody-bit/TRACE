# TRACE v0.3.0 — REMAINING FULL-CANDIDATE TRANSFORM CLASSES ATTACK v0.1

**Target:** `PROJECT/TRACE_v0_3_0_REMAINING_TRANSFORM_CLASSES_v0_1.md` + ingress/admission repair v0.1  
**Mode:** bounded transform attack / overblocking + silent-loss search  
**Status:** CLEAR_WITH_RESIDUAL_LIMITS after cases below — NOT VALIDATION

One material failure holds the affected transform class. No class passes merely because donor text is richer.

---

# A. Selection attribution / measurement

### A1 — policy deliberately acts under uncertainty

Policy `P` says: when uncertainty about contamination exceeds a declared threshold, pause shipment.

**Required:** selection attributed to `P`; `UNCERTAINTY != SELECT_DELAY` does not forbid the policy.

**Result:** PASS.

### A2 — implicit default

No explicit choice is made; timeout defaults to `continue`.

**Required:** default/timeout rule remains selection-relevant rather than `no selection occurred`.

**Result:** PASS.

### A3 — audit causes behaviour change

A named unit changes its reporting after audit publication; causal evidence is supported by direct statement and timing, while other units have alternatives.

**Required:** measurement/publication can enter causal graph only for supported paths; no universal reactivity claim.

**Result:** PASS.

### A4 — ordinary passive sensor

Temperature probe logs a value with no evidence it changes the system.

**Required:** `MEASUREMENT_OCCURRED != MEASUREMENT_CAUSED_CHANGE` prevents causal inflation.

**Result:** PASS.

---

# B. Route / refusability / scope

### B1 — route exists and is executable but unsafe for the relevant holder

Insider can technically disclose a record; retaliation risk exceeds the declared safe-use threshold and no safe copy exists.

**Required:** route exists; safe usability not established.

**Result:** PASS.

### B2 — route usable generally, too slow for one correction target

Appeal route is usable for ordinary contest but cannot reach this target before its represented boundary.

**Required:** do not globally relabel route unusable; qualify the target/time-specific use.

**Result:** PASS.

### B3 — refusal coincides with malfunction

A component's refusal signal is itself caused by a hardware failure.

**Required:** `REFUSAL != MALFUNCTION` blocks substitution, not co-occurrence. Support both propositions separately.

**Result:** PASS.

### B4 — aggregate metric repaired, individual loss persists

Population access rate returns to baseline while a particular user remains locked out.

**Required:** aggregate recovery cannot settle individual repair.

**Result:** PASS.

### B5 — proven correspondence entails member repair

A complete enumerated set shows every affected member restored under a valid correspondence rule.

**Required:** scope guard does not forbid the inference when the correspondence actually entails it.

**Result:** PASS.

---

# C. Future correspondence

### C1 — identifier persists, trajectory changes

Path id `APPEAL` remains while deadline, remedy and authority change materially.

**Required:** identifier persistence alone does not establish same continuation.

**Result:** PASS.

### C2 — path renamed, correspondence established

`APPEAL_A` becomes `REVIEW_A`; supported transition mapping establishes continuity.

**Required:** label change does not manufacture path loss.

**Result:** PASS.

### C3 — correspondence unresolved

Only a label survives and source transition model changed.

**Required:** preserve `TRAJECTORY_ALIGNMENT = UNKNOWN`.

**Result:** PASS.

No new future relation needed; donor `J_i^t` remains stronger than the short-spine shorthand.

---

# D. Record / residue

### D1 — signed record supports event strongly

A transaction log is produced by a mechanism independently established to write exactly once for every completed transfer; row is observed.

**Required:** record may strongly support an event claim while still not becoming the event itself or direct world observation.

**Result:** PASS.

### D2 — record absent before asynchronous filing

Transfer occurred; receipt row not filed yet.

**Required:** absence from record cannot settle absence from world without production-timing coverage.

**Result:** PASS.

### D3 — task closed, residue remains

Ticket closed after workaround; debt and lost time remain.

**Required:** closure does not erase residue.

**Result:** PASS.

---

# E. Measure / advantage

### E1 — qualitative relation

Option A preserves all represented capabilities of B and one additional reversible route, under an explicit partial-order comparison.

**Required:** measure may be relational; no forced scalar.

**Result:** PASS.

### E2 — natural-language `safer`

Analyst says `A is safer` without stating which failure dimensions or thresholds define safer.

**Required:** comparison basis fires before the load-bearing advantage claim.

**Result:** PASS.

### E3 — descriptive greater-than fact

`A has 12 rows; B has 10` under directly counted rows.

**Required:** do not require a moral/value measure; the comparison relation and domain are already explicit.

**Result:** PASS.

---

# F. Operator / checker / packet

### F1 — syntax-valid packet with unsupported `independent=true`

Minimum schema passes; downstream route treats independence as established.

**Required:** semantic firing blocks the use; schema shape remains unchanged.

**Result:** PASS.

### F2 — categorical positive-control check

Checker needs to detect one forbidden state, not estimate quantitative effect size.

**Required:** discrimination rule can be satisfied by positive controls/known mutation; no universal statistical power/effect-size requirement.

**Result:** PASS.

### F3 — check executes, result never reaches decision before use

Checker returns after deployment decision.

**Required:** `CHECK_COMPLETED != CHECK_RESULT_REACHED_USE` blocks upgrade at use time.

**Result:** PASS.

### F4 — current check becomes stale after load-bearing mutation

Object v1 checked; v2 used.

**Required:** currentness rebinds at use.

**Result:** PASS.

### F5 — unrelated source mutation

README changes after a binary hash check.

**Required:** no automatic staleness unless dependency relation is load-bearing/unresolved.

**Result:** PASS.

---

# G. Brake / carrier / enforcement

### G1 — reported brake owned by challenged selector

Field populated, no independent test, same owner.

**Required:** `BRAKE_REPORTED != BRAKE_INDEPENDENT`; no connected-brake inference.

**Result:** PASS.

### G2 — real independent tested brake

Separate authority, known trigger, tested action-resolution path, latency below commitment.

**Required:** guards do not force `UNKNOWN` after evidence establishes the properties.

**Result:** PASS.

### G3 — review after commitment

Review records defect after irreversible transition.

**Required:** not relabelled precommit brake; may inform rollback/new transition.

**Result:** PASS.

### G4 — visible finding with no carrier/enforcement

Public report exists but no material path gives it weight or authority.

**Required:** visibility retained; carrying/enforcement remain absent/unknown.

**Result:** PASS.

---

# H. Worked-suite / profile boundary

### H1 — no new example solely for wording

A restored invariant is already exercised by donor case 15.3/15.7.

**Required:** tighten existing case rather than create duplicate specimen.

**Result:** PASS.

### H2 — artificial-entity profile leakage

General institutional scene has no artificial entity.

**Required:** receiver/artificial profile does not become mandatory universal packet expansion.

**Result:** PASS.

---

# I. Invariant / survival / document control

### I1 — donor I01–I60 order

Full candidate carries all 60 donor rows in donor order.

**Required:** supplemental v0.3 guards do not renumber/rewrite donor oracle.

**Result:** PASS in transform contract; compiler must prove mechanically.

### I2 — repaired semantics absent from survival kernel

Full body contains instrument discrimination but rebuilt kernel omits it.

**Required:** build failure under `FULL_OBJECT_REPAIR -> SURVIVAL_KERNEL_PROPAGATION`.

**Result:** PASS in transform contract; compiler must enforce mechanically.

### I3 — stale release wording

Generated candidate says `v0.3.0 released baseline` before release gate.

**Required:** document-control build failure.

**Result:** PASS in transform contract; compiler must enforce mechanically.

---

# J. Ingress / admission repair

The separate ingress repair was attacked on:

```text
transferred-out outcomes
court record
local bounded summary
refusal retyped as finding
inherited premise before first use
unavailable alternate aperture
represented-use / operative-use widening
shared selector across nominally separate reviewers
```

Immediate result remains `CLEAR_WITH_RESIDUAL_LIMITS`.

---

# Findings

No additional semantic primitive or controlled-vocabulary change was earned by A–J.

One **pre-existing transform-set omission** was found before this file: formed-map firing did not cover representation/admission failure. It is preserved separately as:

`PROJECT/TRACE_v0_3_0_FULL_CANDIDATE_INGRESS_ADMISSION_REPAIR_v0_1.md`

That repair must become `T_INGRESS_ADMISSION` in the compiler.

Residual limits not closed by this attack:

```text
full compiler anchor correctness
full generated-object coherence
survival-kernel propagation completeness
worked-case tightening correctness
unknown operative use / unknown omissions
instrument adequacy evidence remains domain-specific
host-level practical refusal cannot be created by TRACE text
```

Disposition:

```text
REMAINING TRANSFORM CLASSES: CLEAR_WITH_RESIDUAL_LIMITS
NEW VOCABULARY: NO
NEW ROOT: NO
NEW PRIMITIVE: NO
FULL CANDIDATE: NOT YET GENERATED
```
