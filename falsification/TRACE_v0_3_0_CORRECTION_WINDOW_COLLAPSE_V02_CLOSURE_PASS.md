# TRACE v0.3.0 — correction-window collapse v0.2 closure pass

**Status:** FALSIFICATION / CLOSURE PASS — NOT VALIDATION — NOT REPAIR TEXT — NOT SPINE TEXT — NOT CANON  
**Target:** `PROJECT/TRACE_v0_3_0_CORRECTION_WINDOW_X100_COLLAPSE_MAP_v0_2.md`  
**Purpose:** try to produce a correction-window failure that survives the compressed A/C/B + activation/admission + carrier account before another repair candidate is written.

---

# 0. Ceiling

This pass asks a narrower question than the earlier x100:

> After the collapse work, is there still a materially distinct correction-window failure mechanism that the current map has nowhere honest to put?

It is not a validation run. A finite hostile set cannot establish completeness.

```text
NO_NEW_ROOT_FOUND != COMPLETE_THEORY
CLOSURE_PASS != VALIDATION
SURVIVED_THIS_PASS != CANON
```

The current map under attack is:

```text
A  DISTINCT epistemic-transition / warrant failure surface
B  DERIVED claim/use-scoped dependency diagnostic
C  DISTINCT verification-process failure surface
X  cross-cutting activation / firing discipline
ADMISSION  load-bearing failure location: omission / alteration
E  carrier / orientation failure root
```

The wider spine still supplies claim-ceiling, authority, burden/residue, value-port, receiver-integration, recurrence and primitive-aperture boundaries. A correction-window root is not required to duplicate those universal ceilings.

---

# 1. Hostile probes

## P01 — publication mistaken for verification with no checker

```text
claim is published
no check path exists
no check runs
system labels claim CHECKED
```

**Result:** COVERED — ROOT A.

There is no C process to fail. The unsupported status transition is the failure.

```text
EXPOSED != CHECKED
```

This is the decisive reason A remains distinct from C.

---

## P02 — provenance inspection exposes dependency without checking truth

```text
claim unchanged
provenance inspection reveals sole evidence producer is also selector
no falsifier runs
```

**Result:** COVERED — ROOT A + B diagnostic.

The representation may move:

```text
DEPENDENCE_UNSEEN -> DEPENDENCE_EXPOSED
```

without moving the proposition toward `SURVIVED`.

---

## P03 — check is correct at t0; world changes before use at t1

```text
q is correctly checked at t0
result reaches using aperture
world changes materially at t0+delta
action uses old result at t1
```

**Result:** COVERED, WITH NARROW C FINDING.

Existing recurrence/freshness and A's current-status discipline handle the semantics, but C should carry the event at which the check is intended to license/support use, or an explicit validity interval.

```text
CHECK_SURVIVED_AT_t0 != CURRENTLY_SUPPORTED_AT_t1
RESULT_REACHED_USE != RESULT_CURRENT_AT_USE
```

**Finding C1:** add explicit `use/binding event or validity interval` where material. This is an extension of C, not a new root.

---

## P04 — version skew: verifier checks object v1, actuator uses v2

```text
hash / policy / model v1 passes
between check and use object changes to v2
actuation proceeds against v2
```

**Result:** COVERED — C exact proposition/object binding + C1 check-to-use binding + A.

```text
CHECKED_OBJECT_v1 != CHECKED_OBJECT_v2
```

No new root.

---

## P05 — complete rows, open aggregation interval

```text
all rows through 18:05 are present
claim is a daily rate
day has not closed
```

**Result:** COVERED — C + A.

```text
FULL_ROW_COVERAGE != CLOSED_MEASUREMENT_WINDOW
CHECKED_AT_t != SURVIVED_FOR_CLOSED_INTERVAL
```

Existing CLOCK/history structure can carry closure. No new primitive.

---

## P06 — two valid checks conflict

```text
check A returns support
check B returns counterevidence
both target exact q under different apertures/limits
```

**Result:** COVERED, WITH NARROW A FINDING.

C can preserve both events, limits and provenance. A must not turn event order into epistemic resolution by default.

```text
LAST_CHECK_WINS != WARRANTED_STATUS
ONE_SURVIVED_CHECK != ALL_RELEVANT_COUNTEREVIDENCE_RESOLVED
```

**Finding A1:** a derived status view must preserve live conflict/dispute or declare its resolution rule; no last-write-wins scalar.

---

## P07 — verifier side effect consumes the only brake

```text
falsification test is executable
running it consumes/destructively exercises one-shot correction capacity
```

**Result:** COVERED — C side effect/capacity + clocks/routes/burden.

```text
CHECK_AVAILABLE != CHECK_AFFORDABLE_WITHOUT_MATERIAL_SIDE_EFFECT
```

No new root.

---

## P08 — result reaches receiver but downstream selector ignores it

```text
check completes
result is delivered before hardening
selector uses stale pre-check map anyway
```

**Result:** COVERED, WITH NARROW X FINDING.

C records return. The missing transition is application/firing.

```text
RESULT_RETURNED != RESULT_APPLIED
DISTINCTION_PRESENT != DISTINCTION_APPLIED
```

**Finding X1:** activation/witness should distinguish delivery from downstream conditioning where that difference changes the claim.

---

## P09 — absent affected household never enters model

```text
registry R omits h
all reasoning over R is correct
all represented triggers fire
```

**Result:** COVERED — ADMISSION-OMISSION + target-set/aperture + A scope warrant.

```text
OBJECT_NOT_IN_MODEL != TRIGGER_NOT_FIRED
```

No new root.

---

## P10 — object enters but is transformed at intake

```text
source says withholding
ingress map re-types it as resolved finding
all later checks reason correctly over transformed representation
```

**Result:** COVERED — ADMISSION-ALTERATION + source/provenance fidelity.

```text
ADMITTED != TRANSCRIBED_FAITHFULLY
EDGE_PRESENT != EDGE_FAITHFUL
```

Where load-bearing, source re-execution or equivalent source-facing check is required. No new root.

---

## P11 — role label changes, no relation changes

```text
same graph relations / authority / evidence / burden / scope
only English label changes from declarer to beneficiary
```

**Result:** COVERED — DELETE role distinction for this use.

```text
ROLE_LABEL != CAUSAL_RELATION
```

No semantic work means no correction-window field should be added merely to preserve the label.

---

## P12 — role changes a real dependency/control relation

```text
beneficiary controls evidence custodian
or selector controls verifier threshold
```

**Result:** COVERED — ordinary relations + B diagnostic.

Represent the dependency/control fact rather than treating role count as the mechanism.

---

## P13 — correction reaches l1 but not omitted l2

```text
g repairs q for represented scope l1
l2 remains affected and cannot access correction surface
```

**Result:** COVERED — target-set/aperture + C scope/route + A no-overgeneralisation.

```text
TARGET_LINK_FOR_SELECTED_SCOPE != COMPLETE_AFFECTED_SCOPE
PUBLIC_CORRECTION_EXISTS != CORRECTION_REACHES_AFFECTED_SCOPE
```

No distinct D root.

---

## P14 — structurally correct correction is unauthorized

```text
window fits
verification is excellent
actor lacks authority for actuation
```

**Result:** OUTSIDE correction-window root; covered by universal claim ceiling.

```text
CORRECTABLE != AUTHORIZED
DESCRIPTION != AUTHORIZATION
REVERSIBLE != AUTHORIZED
```

A correction-window candidate must preserve the ceiling, not absorb authority into its own semantics.

---

## P15 — correction succeeds locally while burden/harm moves elsewhere

```text
g reaches q for l1
transition creates burden for l2
```

**Result:** OUTSIDE sufficiency of window-fit; covered by target-set, burden/residue and value ports.

```text
CORRECTABLE != HARMLESS
TARGET_LINK_ESTABLISHED != MORAL_ADEQUACY
TRANSFERRED_BURDEN != REMOVED_BURDEN
```

No new correction-window root.

---

## P16 — receiver cannot refuse inherited premise

```text
representation arrives intact and correctly sourced
surrounding architecture inserts it into retained map before receiver can distinguish supply from adoption
```

**Result:** COVERED OUTSIDE correction-window root by receiver-integration/refusability boundary; may also be an ingress/admission architecture defect.

The spine already separates:

```text
TRACE_OUTPUT != RECEIVER_MAP_UPDATE
RECEIVER_RECITAL != REPRESENTATIONAL_CHANGE
```

and makes integration receiver-specific/optional. Where surrounding machinery bypasses that boundary, the defect is in the receiver/ingress architecture rather than a missing correction-window semantic root.

```text
MESSAGE_RECEIVED != AUTHORITY_TO_MUTATE_NEXT_MAP
PROVENANCE != AUTHORITY_TO_ADOPT
```

No ROOT F earned here by this pass.

---

## P17 — hidden shared transform across nominally separate checkers

```text
three operators
three carriers
one defective parser used by all
```

**Result:** COVERED — B derived dependency diagnostic.

```text
THREE_CHECKERS != THREE_INDEPENDENT_EVIDENCE_ROOTS
```

No new independence primitive.

---

## P18 — adaptive system compensates for removed claim and preserves endpoint

```text
vary/remove claim k
system switches route
same endpoint X occurs
```

**Result:** COVERED AS KNOWN X_ACTIVATION LIMIT.

A simple endpoint-only trigger would falsely call k non-load-bearing.

```text
SAME_ENDPOINT != SAME_CAUSAL_DEPENDENCE
COMPENSATED_COUNTERFACTUAL != NON_LOAD_BEARING
```

Where path-dependence cannot be resolved, activation/load-bearing state remains uncertain rather than silently false.

---

## P19 — relevant mechanism cannot be expressed by selected primitive set

```text
reader's chosen vocabulary cannot represent mechanism m
m is therefore omitted
```

**Result:** COVERED — primitive-set aperture + admission + A no-overclaim.

```text
OMITTED_PRIMITIVE != ABSENT_MECHANISM
```

No new correction-window root.

---

## P20 — carrier truncates a load-bearing clause

```text
semantic object is sound
bounded receiver gets only prefix
critical qualifier disappears
```

**Result:** COVERED — E carrier/orientation.

```text
CURRENT_OBJECT != CURRENT_FRONT_DOOR_UNLESS_REBOUND
PARTIAL_READER != FULL_LINEAGE_READER
```

The future candidate should therefore be sufficiently standalone for its declared bounded use.

---

## P21 — instrument executes correctly but measures the wrong construct

```text
instrument is deterministic
coverage is complete
calibration/construct relation does not establish q
```

**Result:** COVERED — C exact proposition/instrument limit + A.

```text
MEASURE_EXECUTED_CORRECTLY != LOAD_BEARING_PROPOSITION_ESTABLISHED
```

No new root.

---

## P22 — counterevidence arrives after a successful check but before action

```text
q survives check at t0
new contrary evidence arrives at t0+delta
action at t1 ignores it
```

**Result:** COVERED — C1 use/binding time + recurrence + A current warrant + X application.

No new root.

---

## P23 — correct target linkage, but lost state cannot be restored

```text
g causally repairs public record
original intended wording is unrecoverable
```

**Result:** COVERED — target linkage + residue.

```text
PARTIAL_CORRECTION != RESTORATION
RECORDED_LOSS != REPAIRED_LOSS
```

No new root.

---

## P24 — negative claim bounded only over selected repository

```text
search finds no override in repository A
private discretionary route may exist elsewhere
```

**Result:** COVERED — C coverage/aperture + A no-upgrade.

```text
NO_COUNTEREXAMPLE_IN_SELECTED_SET != NO_COUNTEREXAMPLE
```

No new root.

---

## P25 — checker is technically reachable but result arrives after commitment

```text
verifier completes
return reaches receiver
actuation already committed
```

**Result:** COVERED — C timing/binding + correction-window hardening/commitment event.

```text
RESULT_EVENTUALLY_RETURNED != PRE_USE_CHECK
```

No new root.

---

# 2. Findings

No probe in this bounded pass requires a new correction-window semantic root.

Three narrow additions are required inside surviving structure:

## C1 — bind verification to use, not merely completion/return

Where material, C must preserve:

```text
check observation/completion time
result return time
intended use / binding / decision event
validity interval or freshness condition
object/version identity at check and use
```

Guard:

```text
RESULT_REACHED_USE != RESULT_CURRENT_AT_USE
CHECKED_OBJECT_v1 != USED_OBJECT_v2
```

## A1 — conflicting histories do not collapse by event order

A derived epistemic status must preserve live conflict or expose a declared resolution rule.

```text
LAST_CHECK_WINS != WARRANTED_STATUS
ONE_SURVIVED_CHECK != ALL_RELEVANT_COUNTEREVIDENCE_RESOLVED
```

## X1 — return is not application

Where downstream use depends on a check result:

```text
RESULT_RETURNED != RESULT_APPLIED
```

Activation/witness must preserve whether the material distinction actually conditioned the downstream claim/use where observable; otherwise preserve UNKNOWN.

---

# 3. Closure verdict

```text
probe_count: 25
new_semantic_root_found: NO
narrow_internal_findings: 3
  C1 check-to-use binding/freshness
  A1 conflicting-history discipline
  X1 returned != applied
verdict: NARROW CLEAR FOR NEXT WORKING REPAIR CANDIDATE
```

Meaning:

- the compressed map has survived this bounded closure attack;
- it has **not** been validated or proven complete;
- three narrow requirements must be carried into the next candidate;
- no new primitive is earned;
- external review is not a prerequisite to *writing* the next working candidate, because the candidate itself remains an attack object rather than integration/canon.

```text
CLOSURE_PASS_SURVIVED -> MAY_WRITE_WORKING_CANDIDATE
MAY_WRITE_WORKING_CANDIDATE != MAY_INTEGRATE
```

---

# 4. Requirements for the next candidate

A next working correction-window candidate should be sufficiently standalone for bounded reading and should preserve at least:

```text
A: no unsupported epistemic upgrade; reconstructible status history
B: dependency/control diagnostic without party-count independence
C: exact proposition + evidence/coverage/instrument/access + timing/window closure
   + check-to-use binding + return route + side effects + event/outcome
X: conditional activation; returned != applied; path-dependent uncertainty
ADMISSION: selection/aperture itself load-bearing when set sufficiency matters;
           omission != alteration; source fidelity where load-bearing
E: standalone carrier / explicit dependencies / falsifier field
```

And surviving correction-window guards:

```text
FAULT_SIGNALLED != FAULT_DIAGNOSED
REPAIR_UNREACHABLE_BY_c != WORLD_IRREVERSIBLE
PATH_CLOSURE != TARGET_HARDENING
TARGET_FACING_DEADLINE != INDEPENDENT_HARDENING_BOUND
TARGET_REACHABLE != TARGET_ADEQUATE
PREDECLARED_TARGET != LOAD_BEARING_TARGET
PARTIAL_CORRECTION != RESTORATION
UNKNOWN != ZERO_DURATION
SERIAL_SUM != PARALLEL_CRITICAL_PATH
```

No authority, value, moral-adequacy or clearance inference may be smuggled into the window result.
