# TRACE Bootstrap Coverage + Falsifier Patch — Hostile Audit Prompt v0.1

Date: 2026-06-18
Status: hostile-audit prompt / not validation / not proof / not active spine / not Kernel v0.3

## 0. Identity and limits header

Before responding, state:

```text
Reviewer identity:
Model / provider:
Session context available:
Memory status:
Tools / web access used:
Confidence limits:
What you did not inspect:
```

Do not present agreement as validation.

Do not praise the project.

Do not smooth over weak claims.

Do not add new stories unless a missing TRACE area genuinely needs one and the existing matrix fails to identify it.

Your job is to attack the Bootstrap Coverage Matrix and the True Inward Falsifier Patch.

## 1. Files to review

Review these files as a package:

```text
03_BOOTSTRAPS/BOOTSTRAP_V2/00_READ_ME_FIRST__BOOTSTRAP_V2.md
03_BOOTSTRAPS/BOOTSTRAP_V2/README.md
03_BOOTSTRAPS/BOOTSTRAP_V2/08_CROSS_CONNECTION_AUDIT_v0_1.md
05_MAPS_AND_ATLASES/TRACE_Bootstrap_Coverage_Matrix_v0_1.md
05_MAPS_AND_ATLASES/TRACE_Bootstrap_True_Inward_Falsifier_Patch_v0_1.md
05_MAPS_AND_ATLASES/TRACE_Universality_Map_v0_1.md
README.md
00_START_HERE/READ_ORDER.md
```

Use active repo files as source of truth. Treat summaries as lower authority.

## 2. Claims under audit

The Coverage Matrix claims:

```trace
Bootstrap_Coverage_Matrix_v0_1 :=
  current_story_coverage_map
  + blanks_named
  + weak_maps_named
  + probe_only_areas_named
  + overmap_risks_named
  + build_order_set
  - validation
  - new_story_promotion
  - operator_promotion
```

The True Inward Falsifier Patch claims:

```trace
Bootstrap_True_Inward_Falsifier_Patch_v0_1 :=
  support_patch
  + six_cluster_falsifiers
  + cross_cluster_story_overreach_falsifier
  + expert_reasoning_falsifier
  + false_universality_falsifier
  - validation
  - relay_file_bloat
  - operator_promotion
```

Main remembered principle:

```trace
Bootstrap_stories_are_for:
  universality
  + cross_domain_translation
not_for:
  validation
  OR story_collection
```

## 3. Main hostile questions

### 3.1 Coverage accuracy

Does the Coverage Matrix correctly classify each TRACE area as mapped, weak, probe-only, translation-only, blank, or overmapped?

Find at least five possible misclassifications.

Use this format:

```text
- Area:
  Current classification:
  Your classification:
  Reason:
  Patch required:
```

### 3.2 Missing areas

What TRACE areas are still missing from the matrix?

Consider:

```trace
possible_missing_areas :=
  repair_after_action
  + responsibility_propagation
  + burden_routing
  + standing_without_personhood_solution
  + evidence_channel_integrity
  + source_authority_gradient
  + enforcement_carrier_failure
  + recursive_agent_training
  + AI_relay_drift
  + mathematical_formalisation
  + domain_expert_correction_route
```

Do not invent areas just to expand the map. Name only omissions that materially change bootstrap coverage.

### 3.3 True inward falsifier quality

For each of the six cluster falsifiers, classify:

```trace
falsifier_quality :=
  true_inward_falsifier
  OR mostly_misuse_guard
  OR too_broad
  OR too_weak
  OR too_strong_and_would_kill_valid_mapping
  OR unclear
```

### 3.4 Expert reasoning falsifier

The patch says:

```trace
expert_reasoning_falsifier :=
  if:
    ordinary_expert_framework
    catches_the_failure_earlier
    + names_better_interventions
    + avoids_TRACE_jargon
    + keeps_accountability_clearer
  then:
    TRACE_mapping_demotes_to_translation_note_or_recognition_aid
```

Is this too harsh, too weak, or correct?

If too harsh, explain how TRACE can still be useful as cross-domain communication even when expert reasoning is better inside one domain.

If too weak, sharpen it.

### 3.5 False universality falsifier

The patch says cross-domain communication fails if the same TRACE phrase changes meaning across domains, erases domain-specific constraints, or prevents local experts from correcting the mapping.

Is this the right falsifier for universality-through-translation?

Classify:

```trace
universality_falsifier_verdict :=
  retain
  OR retain_with_patch
  OR too_weak
  OR too_strong
  OR confused
```

### 3.6 Story addition rule

Does the Coverage Matrix correctly block story collection?

Test this rule:

```trace
story_addition_rule :=
  add_story_only_if:
    it_maps_blank_or_weak_TRACE_area
    + has_historical_echo
    + has_source_anchor
    + has_boundary
    + has_true_inward_falsifier
    + can_demote_existing_claim
  not_if:
    merely_confirms_TRACE
    OR adds_cultural_flavour
    OR creates_fandom_dependency
```

Is it usable, too restrictive, too permissive, or missing a requirement?

### 3.7 Bootstrap priority order

The current proposed priority order is:

```trace
priority_order :=
  1_reader_empathy_pass
  + 2_true_inward_falsifiers
  + 3_Groundhog_Day_AI_relay_loop_patch
  + 4_Bootstrap_worked_navigation_comparison
  + 5_story_to_TRACE_coverage_matrix_hostile_audit
```

But the true inward falsifier patch was built first.

Should the next move be:

```trace
next_move :=
  reader_empathy_pass
  OR patch_falsifiers_after_audit
  OR worked_navigation_comparison
  OR Groundhog_Day_AI_relay_patch
  OR stop_and_consolidate
```

Give one answer and justify it.

## 4. Required output format

Use this structure:

```text
VERDICT: retain / retain_with_patch / demote / archive

TOP 5 MISCLASSIFICATIONS OR OMISSIONS:
1.
2.
3.
4.
5.

FALSIFIER QUALITY TABLE:
| Cluster / cross-cluster rule | Verdict | Reason | Required patch |
|---|---|---|---|

TOP 5 DRIFT RISKS:
1.
2.
3.
4.
5.

TOP 5 STRENGTHS THAT SURVIVE HOSTILE READING:
1.
2.
3.
4.
5.

REQUIRED PATCHES:
- file:
  section:
  problem:
  proposed replacement or instruction:

NEXT MOVE:
Choose exactly one.

FINAL COMPRESSION:
```

The final compression must be no more than 12 lines.

## 5. Hard constraints

Do not validate TRACE.

Do not treat story recognition as evidence.

Do not promote any story, probe, or cluster into an operator.

Do not treat the falsifier patch as already installed in Bootstrap V2.

Do not collapse reader-empathy issues into falsification issues.

Do not add more story carriers unless a blank or weak mapping requires one.

Do not allow universality language to become a claim that TRACE overrides local domain expertise.

End.
