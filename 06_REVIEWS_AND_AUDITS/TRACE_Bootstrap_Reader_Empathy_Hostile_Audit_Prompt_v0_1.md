# TRACE Bootstrap Reader Empathy Pass — Hostile Audit Prompt v0.1

Date: 2026-06-18
Status: hostile-audit prompt / not validation / not proof / not active spine / not Kernel v0.3

## 0. Identity and limits header

Before responding, state:

```text
Reviewer identity:
Model / provider:
Session context available:
Memory status:
Tools / web access used:
Confidence limits:
What you did not inspect:
```

Do not present agreement as validation.

Do not praise the project.

Do not add new story carriers unless the reader-entry patch demonstrably fails without one.

Your job is to attack the Reader Empathy Pass before it is inserted into Bootstrap V2.

## 1. Files to review

Review these files as a package:

```text
05_MAPS_AND_ATLASES/TRACE_Bootstrap_Reader_Empathy_Pass_v0_1.md
03_BOOTSTRAPS/BOOTSTRAP_V2/00_READ_ME_FIRST__BOOTSTRAP_V2.md
03_BOOTSTRAPS/BOOTSTRAP_V2/README.md
03_BOOTSTRAPS/BOOTSTRAP_V2/08_CROSS_CONNECTION_AUDIT_v0_1.md
05_MAPS_AND_ATLASES/TRACE_Bootstrap_Coverage_Matrix_v0_1.md
05_MAPS_AND_ATLASES/TRACE_Bootstrap_True_Inward_Falsifier_Patch_v0_1.md
```

Use active repo files as source of truth. Treat summaries as lower authority.

## 2. Claims under audit

The Reader Empathy Pass claims:

```trace
Bootstrap_Reader_Empathy_Pass_v0_1 :=
  six_cluster_reader_entry_insertions
  + carrier_context_before_TRACE_translation
  + boundary_visible
  - new_stories
  - proof_claims
  - relay_pack_edit_yet
```

The required reader-entry standard is:

```trace
reader_entry_valid_if:
  reader_can_follow_pattern_without_prior_fandom_knowledge
  + story_context_is_minimal_but_sufficient
  + analogy_boundary_visible
```

## 3. Main hostile questions

### 3.1 Does each insertion actually help reader entry?

For each cluster, classify:

```trace
reader_entry_verdict :=
  retain
  OR retain_with_compression
  OR too_long
  OR too_fandom_dependent
  OR too_abstract
  OR adds_theory_bloat
  OR needs_patch
```

### 3.2 Does any insertion smuggle validation or story-proof?

Find every sentence that could be read as:

```trace
overclaim_risks :=
  story_recognition_as_evidence
  OR film_as_policy_proof
  OR fictional_AI_as_current_AI_personhood_proof
  OR historical_case_flattened_into_story
  OR TRACE_validation_by_carrier_density
```

### 3.3 Does any insertion over-compress the story?

A reader-entry block should be short, but not misleading. Identify where a story has been compressed so far that the pattern becomes inaccurate or unfair.

### 3.4 Does any insertion duplicate what the cluster already says?

If the cluster already has enough reader orientation near the top, say so. Do not add text merely to satisfy process.

### 3.5 Should these blocks be installed now?

Choose exactly one:

```trace
installation_verdict :=
  install_all_minimal_blocks
  OR install_compressed_blocks_only
  OR install_none_keep_support_patch
  OR patch_then_install
  OR hostile_audit_again
```

## 4. Required output format

Use this structure:

```text
VERDICT: retain / retain_with_patch / demote / archive

CLUSTER TABLE:
| Cluster | Verdict | Main issue | Required patch |
|---|---|---|---|

TOP 5 RISKS:
1.
2.
3.
4.
5.

REQUIRED PATCHES:
- file:
  section:
  problem:
  proposed replacement or instruction:

INSTALLATION VERDICT:
Choose exactly one.

FINAL COMPRESSION:
```

The final compression must be no more than 12 lines.

## 5. Hard constraints

Do not validate TRACE.

Do not treat story recognition as evidence.

Do not add new story carriers.

Do not bloat the relay pack.

Do not collapse reader-entry work into falsifier work.

Do not erase the existing cluster nuance just to make a simpler opening paragraph.

End.
