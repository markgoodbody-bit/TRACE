# TRACE Generative Syntax First Example Toy Status Patch v0.1

Date: 2026-06-18
Status: toy-status patch / simple Merge worked example / term cooling / not validation / not proof / not linguistic theory / not full grammar / not grammar implementation / not operator promotion / not Kernel v0.3

## 0. Control header

This file responds to `TRACE_Generative_Syntax_First_Example_Hostile_Audit_v0_1.md`.

It patches the formal status of `TRACE_Generative_Syntax_Simple_Merge_Worked_Example_v0_1.md` before any second syntax example.

It does not add a second syntax example.

It does not claim a formal derivation.

It does not claim a language-specific grammar.

It does not implement a parser.

```trace
Syntax_First_Example_Toy_Status_Patch_v0_1 :=
  Merge_not_fully_formalised
  + surface_readout_not_syntactic_object
  + label_status_unresolved
  + feature_dependency_gap_visible
  + no_judgment_or_language_specific_claim
  + loss_register_claim_effects
  -> first_syntax_probe_terms_cooled
  - validation
  - full_grammar
  - grammar_implementation
```

## 1. Patch target

```trace
patch_target :=
  TRACE_Generative_Syntax_Simple_Merge_Worked_Example_v0_1
```

This patch does not delete or replace the worked example. It controls how the worked example must now be read.

## 2. Merge status

`External_Merge(D_the, N_cat) -> SO1` must be read as a toy operation label for translation probing.

```trace
External_Merge_status :=
  toy_operation_label_for_translation_probe
  not:
    fully_formalised_Minimalist_operation
    OR source_backed_definition
    OR grammar_rule
    OR implemented_derivation_step
    OR proof_of_Merge_translation
```

Allowed reading:

```trace
allowed_Merge_reading :=
  internal_test_of_operation_vs_generic_combination
```

Not allowed:

```trace
not_allowed_Merge_reading :=
  formal_operation_definition_established
  OR grammar_fragment_established
  OR parser_step_implemented
  OR language_specific_derivation_proven
```

## 3. Surface readout status

The surface readout placeholder must not be treated as the syntactic object itself.

```trace
surface_readout_status :=
  interface_placeholder
  not:
    syntactic_object_itself
    OR full_derivation
    OR judgment_claim
    OR language_specific_analysis
    OR proof_of_phrase_structure
```

Allowed reading:

```trace
allowed_surface_reading :=
  surface_string_used_to_test_surface_vs_syntactic_object_distinction
```

Failure:

```trace
surface_readout_failure :=
  SO1_collapses_to_words_in_order
```

## 4. Label/head status

The worked example leaves head/label status unresolved. This must remain status-affecting.

```trace
label_status :=
  unresolved_in_this_probe
  not:
    head_projection_claim
    OR phrase_category_claim
    OR labelling_algorithm
    OR complete_syntactic_object_theory
```

Consequence:

```trace
label_status_consequence :=
  no_labelling_theory_claim
  + no_phrase_category_claim
  + no_headedness_claim
  + no_complete_derivation_claim
```

## 5. Feature dependency status

Feature dependencies are not modelled in the first worked example.

```trace
feature_dependency_status :=
  not_modelled_gap
  not:
    checked
    OR valued
    OR preserved
    OR solved
    OR silently_inferred
```

Consequence:

```trace
feature_dependency_consequence :=
  no_feature_checking_claim
  + no_Agree_claim
  + no_selectional_feature_claim
  + feature_gap_must_remain_visible
```

## 6. Judgment and language-specific status

The first syntax worked example does not use judgment data or source-backed language-specific analysis.

```trace
judgment_status :=
  not_tested
  not:
    grammaticality_claim
    OR acceptability_claim
    OR language_specific_grammar_claim
    OR language_specific_analysis
```

Consequence:

```trace
judgment_consequence :=
  no_grammaticality_claim
  + no_acceptability_claim
  + no_native_speaker_judgment_claim
```

## 7. Hierarchy status

The hierarchy profile is a toy binary-branching placeholder.

```trace
hierarchy_status :=
  toy_binary_branching_placeholder
  not:
    full_phrase_structure_theory
    OR labelled_tree_claim
    OR labelling_solution
    OR cross_linguistic_hierarchy_claim
```

Consequence:

```trace
hierarchy_consequence :=
  can_test_hierarchy_vs_linear_order
  but:
    cannot_claim_full_tree_theory
    + cannot_claim_labelled_syntactic_structure
```

## 8. Loss register claim effects

The loss register must change allowed claims.

```trace
loss_register_claim_effects :=
  no_label_theory -> no_phrase_category_claim
  + no_feature_calculus -> no_feature_dependency_claim
  + no_Agree_or_Internal_Merge -> no_dependency_or_movement_claim
  + no_judgment_data -> no_grammaticality_claim
  + no_parser -> no_implementation_claim
  + no_cross_linguistic_data -> no_general_language_claim
```

No consequence means audit theatre.

```trace
no_consequence_loss :=
  invalid_loss_register_entry
```

## 9. Updated back-translation requirement

The back-translation target remains valid only under toy status.

```trace
BT_Syntax_required_recovery_after_patch :=
  toy_syntax_scope
  + lexical_items_D_the_and_N_cat
  + External_Merge_as_toy_operation_label
  + SO1_as_toy_syntactic_object
  + hierarchy_profile_as_toy_binary_branching_placeholder
  + surface_readout_as_interface_placeholder
  + label_status_unresolved
  + feature_dependency_not_modelled
  + no_judgment_claim
  + no_full_grammar_claim
  + loss_register
```

Failure:

```trace
BT_Syntax_fails_if:
  returns:
    words_combined_only
    OR language_specific_analysis
    OR formal_derivation_claim
    OR judgment_claim
```

## 10. Updated status

```trace
Syntax_first_example_status_after_patch :=
  INTERNAL_PROFILE_CANDIDATE
  + NATIVE_REVIEW_REQUIRED
  + TOY_STATUS_COOLED
  for:
    one_step_External_Merge_translation_probe_only
```

Meaning:

```trace
status_means :=
  useful_internal_translation_probe
  + Merge_vs_generic_combination_test
  + syntactic_object_vs_surface_string_test
  + hierarchy_vs_linear_order_test
  + not_formal_derivation
  + not_grammar
  + not_parser
```

## 11. Gate before second syntax example

A second syntax example may proceed only if it preserves this patch.

```trace
second_syntax_example_gate :=
  toy_status_patch_complete
  + Merge_not_fully_formalised_boundary_active
  + surface_readout_not_syntactic_object_boundary_active
  + label_gap_visible
  + feature_dependency_gap_visible
  + no_judgment_claim_boundary_active
  + native_syntax_profile_family_named
  + loss_register_required
  + back_translation_target_written
```

Possible next syntax example:

```trace
possible_next_syntax_example :=
  simple_Internal_Merge_or_movement_contrast
  OR
  Merge_vs_linear_order_contrast
  only_as:
    toy_schematic_translation_probe
```

Alternative:

```trace
alternative_next :=
  syntax_first_example_consolidation
```

## 12. Updated must-not-claim

```trace
must_not_claim_after_patch :=
  formal_derivation_claim
  OR source_backed_Merge_definition
  OR language_specific_grammar_claim
  OR grammaticality_claim
  OR phrase_category_claim
  OR feature_checking_claim
  OR parser_implementation
  OR cross_linguistic_claim
```

## 13. Updated allowed claim

```trace
allowed_claim_after_patch :=
  TRACE_has_internal_toy_External_Merge_profile_probe
  + Merge_language_has_been_cooled
  + surface_readout_leak_is_blocked
  + label_gap_is_visible
  + feature_dependency_gap_is_visible
  + no_judgment_or_language_specific_boundary_is_active
  + second_syntax_example_may_proceed_only_under_gate
  - validation
  - full_grammar
  - grammar_implementation
```

## 14. Final compression

```trace
Syntax_First_Example_Toy_Status_Patch_v0_1 :=
  Merge := toy_operation_label_not_formal_definition
  surface := interface_placeholder_not_syntactic_object
  label := unresolved_gap
  features := not_modelled_gap
  judgments := no_judgment_or_language_specific_claim
  loss := allowed_claim_affecting
  status := INTERNAL_PROFILE_CANDIDATE + NATIVE_REVIEW_REQUIRED + TOY_STATUS_COOLED
  next := second_syntax_example_or_consolidation_under_gate
  - validation
  - full_grammar
  - grammar_implementation
```

End.
