# Archived workflow definitions

These workflows are retained as historical build and falsification machinery.
They are no longer active because their path contracts describe the previous
repository layout.

The active workflow verifies the current root candidates, selected v0.3
package, public blind archive boundary and v0.2.7 baseline identities. An old
green run should not be treated as evidence about a later repository state.
