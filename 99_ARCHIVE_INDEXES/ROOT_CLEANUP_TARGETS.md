# Root Cleanup Targets

This file records the current cleanup target: root should contain only `README.md` and directories.

Manual upload dump files at root should be removed from the current tree, not treated as current architecture.
